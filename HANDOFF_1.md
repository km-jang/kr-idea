# kridea — 국내장 아이디어 대시보드 인수인계 (현재 V4-260724 차트 · 문서 2026-07-28)

## 🏷 버전 이름 규칙 — 프로젝트명 "kridea", 형식 `Vn-yymmdd`

버전은 `Vn-yymmdd` (예: V4-260724). 숫자는 순번, 뒤 6자리는 그 버전이 **완성·배포된 날짜**.
새 세션은 "kridea Vn 작업 시작"으로 열고, 작업이 끝나 배포하는 날 날짜를 붙여 `Vn-yymmdd`로 확정한다. HANDOFF도 그 이름으로 남긴다.

| 버전 | 이름 | 내용 |
|---|---|---|
| V1-260710 | 기본 | 초기 대시보드 (수급·밸류·모멘텀·공시 4축) |
| V2-260716 | 스윙 | 5일선 스윙 레이더 + 청산 도우미 |
| V3-260717 | 안정 | 안정화 1·2탄 (자동복구·실행성적표·크론 정비) |
| V4-260724 | 차트 | 차트 카드 + 휴장일 달력 + 엠대시 정리 ← **현재 배포판** |
| V5-(작업중) | 단골 | (다음) 화면 다이어트 + 차트 커버리지 확대 + 단골 배지 [M2 예정] |

> 다음 세션 첫 마디 예: "km-jang/kr-idea 대시보드 **kridea V5** 작업 시작. CLAUDE.md, HANDOFF.md 먼저 읽어줘."
> (V5 완성해 배포하는 날, 예를 들어 8월 3일이면 **V5-260803 단골**로 확정)

---

> 새 세션(방)에서 이 프로젝트를 이어서 작업하기 위한 요약본.
> **새 AI에게 제일 먼저**: 저장소의 `CLAUDE.md`(작업 지침) · `PLAYBOOK.md`(장애 복구) ·
> `ROADMAP.md`(남은 기획) · `IDEAS.md`(기획 뱅크) · `UPGRADE-PLAN.md` / `UPGRADE-PLAN-MAX.md`(유료 고도화) ·
> `CHART-CARD-PLAN.md`(차트 카드 기획)를 읽고 시작할 것.

## 0. 새 세션 시작 첫 마디 (사용자용 복붙 문구)

> "km-jang/kr-idea 저장소의 국내주식 대시보드를 이어서 작업할 거야.
> 먼저 CLAUDE.md, PLAYBOOK.md, ROADMAP.md, HANDOFF.md를 읽고 시작해줘.
> 나는 비개발자니까 모든 안내는 클릭 단위로 쉽게 해줘."

## 1. 프로젝트 한눈에

- **무엇**: 국내 주식(KOSPI/KOSDAQ) 투자 아이디어 대시보드 + 텔레그램 자동 브리핑
- **소유자**: 비개발자(한국어). 모든 안내는 **클릭 단위**로, 전문용어 없이
- **웹**: https://km-jang.github.io/kr-idea/ (GitHub Pages, 공개 저장소 km-jang/kr-idea, main 루트)
- **비용**: 서버비·API비 완전 0원 (GitHub 무료 + 무료 쿼터 API만)
- **구조**: GitHub Actions(크론 10개) → collect.py 수집·채점 → data.json 커밋 →
  Pages가 대시보드 표시 + notify.py가 텔레그램 발송. 단일 파일 HTML(외부 JS 라이브러리 없음).

## 2. 절대 규칙 (CLAUDE.md 전문 참조, 어기면 시스템이 깨진다)

1. **API 비용 0원** (유료 API·LLM API 영구 금지). 무료 공식 API만(네이버 개발자·OpenDART).
   문맥 분석이 필요하면 자동화하지 말고 사용자가 Claude/Gemini 대화에 직접 붙여넣기.
   ※ 단, 유료 고도화는 UPGRADE-PLAN에 기획만 되어 있고 아직 미실행 (규칙 8 개정 전까지 0원 유지).
2. **data.json 스키마 하위호환** — 키 추가만 허용, 삭제·개명 금지 (폴백 유지).
3. **모든 수정 후 `python test_parsers.py` 전체 통과** (현재 **97개**). 테스트는 반드시 고정 픽스처만.
4. **한국 색 관례: 빨강=상승, 파랑=하락.**
5. **0픽셀 원칙** — 신규 기능은 상시 화면을 늘리지 않는다(클릭 확장·조건부 표시만).
6. 네이버 예의(REQUEST_DELAY 0.12s 유지), 시크릿 노출 금지, 점수 튜닝은 상단 CONFIG 값만.
7. **엠대시(—) 사용 금지** — 사용자 화면·텔레그램 텍스트엔 가운뎃점(·)·쉼표·콜론만 (2026-07-24 확정).

## 3. 배포 방법 (특수 — AI 세션에서 git push 막힘)

- 수정 파일을 **ZIP으로 전달** → 사용자가 GitHub 웹 `Add file → Upload files`로 드래그·덮어쓰기.
- `.github/` 안의 update.yml만은 드래그 불가 → 웹 편집 링크
  (github.com/km-jang/kr-idea/edit/main/.github/workflows/update.yml)로 붙여넣기 안내.
- 전달 전 반드시: 테스트 전체 통과 + (UI면) 라이트/다크/모바일(390px) 렌더링 + 콘솔에러 0.
- 반영 후 raw.githubusercontent.com 또는 `git fetch`로 원격 일치 검증. "파일 몇 개 올리는지" 명시.

## 4. 파일 지도

| 파일 | 역할 |
|---|---|
| `collect.py` (~109KB) | 수집·채점 파이프라인. 튜닝은 상단 CONFIG / CONFIG_SCREENS / CONFIG_MINES / CONFIG_SWING |
| `notify.py` | 텔레그램 발송 (아침 / --evening / --weekly). 휴장일 게이트 포함 |
| `closing_scan.py` | 14:48 종가매매 스캔 + --pulse(점심 맥박). 휴장일 게이트 포함 |
| `holidays_kr.py` | KRX 휴장일 달력 (2026 전체 + 제헌절 재지정). 휴장일 조용히 스킵 |
| `ops_log.py` / `ops.json` | 실행 성적표 기록기 / 장부 (야간 자가점검이 매일 기록) |
| `index.html` | 메인 대시보드. 신호등 클릭 시 실행 성적표, 종목 클릭 시 퀵메뉴(📊차트 카드 포함) |
| `chart.html` | 📊 차트 카드 (120일 차트 + 판정 4줄). data.json `chart_pack` 사용 |
| `swing.html`·`dividend.html`·`insider.html`·`mine.html` | 스윙·배당·내부자·지뢰 하위 페이지 |
| `test_parsers.py` | 오프라인 테스트 97개 |
| `update.yml` | Actions 워크플로 (크론 10개). 저장소 경로 `.github/workflows/update.yml` |
| `data.json` / `history/*.json` | 수집 결과 / 일별 스냅샷(삭제 금지) |
| `CLAUDE.md`·`PLAYBOOK.md`·`ROADMAP.md`·`IDEAS.md` | 지침·복구·기획·아이디어 뱅크 |
| `UPGRADE-PLAN.md`·`UPGRADE-PLAN-MAX.md`·`CHART-CARD-PLAN.md` | 유료 고도화·풀리빌드·차트카드 기획서 |

## 5. 최근 완료 작업 (2026-07-16 ~ 07-28)

- **📈 5일선 스윙 레이더 (swing.html)** + 청산 도우미(ma5/ma20 전 종목 탑재) + 저녁 브리핑 스윙 라인.
- **안정화 1탄** (07-16): 19:40 예비 수집 신선도 판정 강화(15:40 룰), 야간 자가점검 자동 복구 승격.
- **안정화 2탄** (07-17): concurrency `cancel-in-progress:false`(지연 수집 강제취소=조용한 유실 차단),
  모든 push에 pull-rebase 재시도, 크론 분(minute) 정각·5분배수 회피(혼잡 지연 감소),
  아침 브리핑 자가복구, **실행 성적표(ops.json)** — 신호등 클릭 시 최근 5영업일 매트릭스.
- **차트 카드 v2 (chart.html)** (07-17): 120일 차트(20·60일선·전고점 저항·지지선) + 판정 4줄
  (추세·수급·관찰·리스크). 대상: 5선 + 스윙 + 워치리스트(약 12종목). collect.py `chart_pack` 수집.
  별점·보조지표·말풍선 전부 뺀 미니멀 확정판.
- **휴장일 달력 (holidays_kr.py)** (07-17): 주말 + 2026 공휴일 16일. 휴장일엔 수집·스캔·저녁요약·
  자가점검이 조용히 쉼, 아침 브리핑엔 안내 한 줄. 제헌절(7/17) 공휴일 재지정 반영.
- **엠대시 제거** (07-24): 사용자 화면·텔레그램 텍스트 엠대시 23개 → 가운뎃점·쉼표·콜론. **업로드 완료.**

**실전 검증**: 7/20~7/24 실행 성적표 대부분 올(O). 폭락장(7/16 -6.4%, 7/20 -4.5%)에서도 무사고.
데이터 정확(코스피 실제 종가와 일치). 안정화 패치로 "누락·유실"은 확실히 해결됨.

## 6. ⚠️ 미해결 / 다음 세션이 먼저 볼 것

- **🔴 헛경보 버그 (미수정, 오류 fix 대기중)**: 2026-07-28 실측. 크론 지연이 자정을 넘기면
  (예: 7/27 밤 자가점검이 00:26 KST에 실행) 자가점검이 "오늘=7/28"로 인식 → 아직 장 안 열린 날의
  데이터가 없다고 판단 → 복구 실패 → 🩺 헛경보 발송 + ops.json에 7/28 전부 X·복구fail 기록.
  **진짜 고장 아님(데이터 정상), 하지만 지연 심할 때마다 반복될 헛경보.**
  → 수정안: 자가점검이 자정 넘겨/새벽에 실행되면 조용히 스킵하는 가드 한 줄(update.yml).
  어차피 아침 08:03 자가복구가 진짜 문제는 잡음. **사용자에게 "지금 고칠까 M2에 넣을까" 물어본 상태.**
- **근본 원인 = GitHub 무료 크론 지연** (1~3시간). 점심 맥박이 15시, 종가 스캔이 17시에 도착해
  두 기능이 타이밍을 놓침. 해결책은 유료/무료 상시 서버(UPGRADE-PLAN Tier2, 오라클 0원 or 헤츠너 월 7천원).

## 7. M2 묶음 (7월 말, "M2 진행해줘"로 착수 — 한 번에 빌드→한 번 업로드)

1. **화면 길이 다이어트** — 공시 60건 나열 → 10건 + 더보기 (메인 세로 11,486px 문제).
2. **차트 커버리지 확대** — 성과 스크리너(수급·밸류) 종목도 차트 보이게 chart_pack 확대(약 12→65종목).
3. **🔁 단골 배지** — 5선·스윙·스크리너 반복 등장 종목에 조건부 배지(기존 신호 리본 재활용, 0픽셀, 새 데이터 없음).
4. (선택) 위 6번 헛경보 가드를 지금 안 고쳤으면 여기 포함.
- **튜닝**: 스윙 성적표·주간 결산 쌓인 것 보고 CONFIG 가중치 1차 조정. 단 폭락↔반등 구간 수치는 신뢰도 낮으니 장 안정 후.

## 8. 대기·보류 (히스토리 축적 후, 9월경)

- **클릭 → 동료주(테마 페어)**: 사용자 요청. 업종 분류는 너무 coarse(K뷰티 다 "경기소비재")라 부적합.
  정답은 **동반 등락 상관(그림자 페어, IDEAS D)** — 히스토리 2~3개월 필요(현재 8일치, 9월 타깃).
  급하면 테마 사전 seed(수작업 부담). 산업분류로는 달바-실리콘투-코스맥스가 안 묶임(제조/유통/ODM 분리).
- 백테스트 프레임 · 어닝 시그널 · 스윙 셋업별 적중률 점수 승격.
- **대기 중 사용자 액션**: watchlist.txt 채우기(아직 비어있음 — 채우면 관심종목 알림+스윙 청산+차트카드 대상 증가).

## 9. 유료 고도화 결정 (UPGRADE-PLAN 참조, 미결)

- **Tier1 AI 분석**(월 500원~5천원, 규칙8 개정 필요) / **Tier2 상시서버**(0원~월7천원, 크론 지연 해결) /
  **Tier3 실시간**(KIS API 무료, 장중 파수꾼). 추천 스위트스팟 = 월 5천원 "표준".
- 풀리빌드(UPGRADE-PLAN-MAX): 월 2~4만원 "1인 퀀트 데스크". 결정은 M2 이후.
- **정체성 메모**: 이 시스템은 중기 가치·수급 도구라 당일 테마 로켓(로봇주 등)은 설계상 안 잡음(안티-FOMO).
  테마 참여를 원하면 실시간(유료)이 필요하고, 이건 철학의 갈림길 — 사용자가 M2 후 판단.

## 10. 장애 시

`PLAYBOOK.md` 증상별 매뉴얼. 핵심 진단: Actions 초록/빨강 → 대시보드 "장 마감 기준" 날짜 →
신호등 클릭해 실행 성적표 매트릭스 확인. "🩺 자가점검" 또는 "🚨 실행 실패" 텔레그램의 링크로 로그 확인.
※ 자정 넘겨 온 🩺 복구실패는 위 6번 헛경보일 가능성 높음 — 데이터 날짜부터 확인.
