# 새 세션 인수인계 요약 (2026-08-31 작성)

> 이 문서는 "지금 막 끝난 세션"이 다음 세션에게 넘기는 **상태 보고서**다.
> 프로젝트 전체 맥락은 여전히 **HANDOFF.md가 정본**이고, 이 문서는 그 위에 얹는 최신 델타다.
> 둘이 다르면 HANDOFF.md를 믿고, 이 문서는 폐기해도 된다.

---

## 0. 새 세션 첫 마디 (복붙용)

```
https://github.com/km-jang/kr-idea

HANDOFF.md → CLAUDE.md → SESSION-HANDOVER-260831.md 순서로 읽고 시작해줘.
파일 조회는 GitHub API가 막혀 있으니 raw.githubusercontent 로 읽고,
index.html 처럼 큰 파일은 잘려서 보일 수 있으니 잘림 여부를 먼저 확인해줘.
```

---

## 1. 프로젝트 한눈에

| 항목 | 내용 |
|---|---|
| 무엇 | 국내(코스피·코스닥) 종목 아이디어 대시보드 + 텔레그램 브리핑 |
| 어디서 도나 | GitHub Actions(수집·발송) + GitHub Pages(화면). 서버 없음 |
| 비용 | **0원 원칙** (규칙 8). 유료 API·LLM API 금지 |
| 유니버스 | 코스피 400 + 코스닥 300 = **700종목** (`CONFIG["kospi_n"]`, `["kosdaq_n"]`) |
| 데이터 흐름 | `collect.py` → `data.json`(봇이 커밋) → Pages 화면 + `notify.py` 텔레그램 |
| 장부 파일 | `data.json`, `history/*.json`, `ops.json`(실행 성적표), `scans.json`, `sent_log.json` |
| 손대면 안 되는 것 | **`ops.json`·`sent_log.json` 직접 수정 금지.** 오염돼도 코드로 무해화한다 |
| 배포 방식 | 로컬 git push 불가. **ZIP으로 만들어 드리면 사용자가 웹 업로드** (커밋 메시지 "Add files via upload") |

### 크론 10개 (KST 기준)

| 시각 | 하는 일 |
|---|---|
| 08:03 / 08:28(예비) | 아침 브리핑 |
| 12:06 / 12:38(예비) | 점심 맥박 |
| 14:48 / 15:07(예비) | 종가 스캔 (`closing_scan.py`) |
| 19:13 / 19:43(예비) | 수집 + 마감 요약 |
| 23:03 | 야간 자가 점검 |
| 일요일 18:07 | 주간 결산 |

---

## 2. 지금 상태 (2026-08-31 11:00 KST 시점)

- **버전: V6.1a-260831 "지각(핫픽스)"** ← 현재 배포판
- **테스트: 114/114 통과**
- `data.json`: `market_date` = **2026-08-28**, 종목 700, `errors` 배열 **비어 있음**, 키 36종
- **크론 지연 나흘째 관찰 중** (사고 5). 8/30 주간 결산이 예약 18:07 → 실제 23:23 KST(약 5.3시간 지연)로 도착했다. GitHub 쪽 문제이고 우리 코드는 정상이다
- 오늘(8/31) 세션에서 V6.1 업로드(09:37) → 발송 전면 실패 발견 → **핫픽스 V6.1a 업로드(10:53)** 로 마감. 깨져 있던 창은 09:37~10:53 KST 약 74분

### 오늘 반드시 확인해야 할 3개 (핫픽스 실전 검증)

1. **12:06 점심 맥박** 도착 여부
2. **14:48 종가 스캔** 도착 여부 + 텔레그램에 **🩸 마감 투매 눌림** 블록이 처음으로 실제로 찍히는지
3. **19:13 수집 + 마감 요약** 도착 여부 ← 이게 오면 `late_notice` 기본 인자 경로까지 실전 통과

셋 다 오면 사고 6은 완전히 닫힌 것이다. 하나라도 안 오면 **먼저 `data.json`의 `errors` 배열부터 볼 것**(체크리스트 0번).

---

## 3. 이번 세션에 한 일 (V5.7 → V6.1a)

| 버전 | 이름 | 핵심 |
|---|---|---|
| V5.7-260821 | 동료 | 🤝 동료주 발굴기(`pair.html`, 브라우저 연산) + 📋 신호 신뢰도 통지표 + 2027 휴장일 선등록 + ETF 절충 |
| V5.8-260821 | 규율 | ⏱ 킬 스위치(`kill_watch`) + 🧪 백테스트 실험실(`backtest.html`) |
| V5.9-260824 | 국면 | 🪜 돌파 국면 추적(`phase_track`) + 📑 리포트 레이더(`reports`) + 🚰 수급 물꼬 검색식 6호(`turnflow`) |
| V6.0-260824 | 눌림 | 🌊 파동 에너지 눌림(`wave_pullback`) + 🩸 마감 투매 눌림 스캔(`dump_stats`) |
| V6.1-260828 | 지각 | 크론 지연 대응 5종 (시간 창 가드 · 자정 보정 · look-ahead 차단 · 지연 발송 머리말 · 성적표 수집시각 열) |
| V6.1a-260831 | 지각(핫픽스) | `notify.py`의 `KST` 미정의로 브리핑 발송이 전부 죽은 것 수정 |

### 새로 생긴 파일 2개

- **`pair.html`** - 동료주 발굴기. 시장 대비 초과수익률로 시차 상관을 브라우저에서 계산 (서버 비용 0원 유지). 날짜 목록은 `data.json`의 잘린 `kospi_trend`가 아니라 **최근 3개월 평일을 만들어 404는 건너뛰는 방식**
- **`backtest.html`** - 백테스트 실험실. `STRATEGY_PRESETS` + `pick_ideas_weighted`를 브라우저에서 미러링. 기본형 +4.1% ≈ 전략 리그 실측 +4.13%로 산식 일치 검증 완료

### 새로 생긴 `data.json` 키 6종

`swing_stats`, `kill_watch`, `phase_track`, `reports`, `wave_pullback`, `dump_stats`
(스키마는 **추가만** 허용 - 규칙 1)

---

## 4. 사고 3건과 교훈 (반드시 읽을 것)

### 사고 4 - 가격 0 불량 레코드가 기능을 통째로 죽임 (8/25)
유니버스를 700으로 늘리자 가격 0.0 · 등락률 -100.0인 ETN 레코드가 섞였고, `phase_track`이 `float division by zero`로 **하루 넘게 조용히 죽어 있었다**.
→ 3중 방어(수집 단계 제거 · 함수 내 가드 · 회귀 테스트)로 해결.
→ **교훈: 부가 기능의 예외는 화면에 안 뜨고 `errors` 배열에만 남는다.** 그래서 검증 체크리스트 0번이 "`errors` 먼저 보기"가 됐다.

### 사고 5 - GitHub 크론 전면 지연 (8/27~, 진행 중)
8/27부터 예약 실행이 5.4~14시간씩 밀렸다. 가벼운 아침 작업도 똑같이 밀렸고 유니버스 확대(8/21) 이후 8/26까지는 정상이었으므로 **우리 코드 원인이 아니다**.
→ V6.1 가드 5종으로 대응.
→ 부수 발견: 7~8월 초 스캔 다수가 16:37~17:58(장 마감 후)에 실행돼 **look-ahead**가 섞여 있었다. 걸러내니 추격 통계 표본 47→28건, 성적은 +0.57%→**+0.85%로 오히려 개선** → 부풀림은 없었고 방법론 결함만 제거됐다.
→ 오염된 `ops.json` 기록은 **고치지 않고 코드로 무해화**했다(장부 직접 수정 금지 원칙).

### 사고 6 - 테스트가 기본 인자 경로를 안 밟아 배포 당일 발송 전면 중단 (8/31, 내 실수)
`notify.py`의 `late_notice`에서 `NameError: name 'KST' is not defined`.
`collect.py`·`closing_scan.py`·`ops_log.py`에 `KST`가 있으니 `notify.py`에도 있으리라 **가정**한 것이 원인.
회귀 테스트가 항상 `now`를 명시적으로 넘겨서 `now or datetime.now(KST)` 경로를 한 번도 밟지 않았고, **114/114 통과 상태로 프로덕션은 1행에서 죽어 있었다.** CLAUDE.md 체크리스트의 "notify 변경 시 `--dry-run`으로 3종 미리보기"도 건너뛰었다.
→ `timezone` import + `KST` 상수 추가, 테스트는 인자 없이 `late_notice(m)` 호출하도록 강화, 커밋본으로 3종 dry-run 전부 확인.
→ **교훈 2개: ① 기본 인자 경로를 밟지 않는 테스트는 테스트가 아니다 ② 체크리스트는 급할수록 지킨다.**

---

## 5. 결정 기록 (다시 꺼내지 말 것)

| 결정 | 내용 |
|---|---|
| ETF/ETN 제외 범위 | `is_index_product`는 **접두어+공백** 또는 이름에 "ETN" 포함일 때만 True. **BNK금융지주가 리트머스 시험지** - 일반 종목으로 남아야 한다 |
| 유니버스 700 | 우선주(코드 끝자리 0 아님)·ETF 제외 후 400/300으로 확대. 네이버 예의(`REQUEST_DELAY` 0.12s)는 유지 |
| 크론 지연 | 우리 코드 문제 아님. 가드만 넣고 **관찰 유지**. 9/4에 재판단 |
| 성적표 오기록 | `ops.json` 오염분은 **그대로 두고** 코드에서 배제 |
| 실시간 알림(마켓스타PRO) | **채택 안 함** - 구조(무료 크론)와 정체성에 안 맞음. 재제안 금지 |
| 완료된 기획 | 삭제하지 않는다. 취소선 + ✅ + 날짜로 남긴다 |

---

## 6. 예약 리마인더 3건

| 날짜 | 내용 |
|---|---|
| **9/4 09:00** | 크론 지연 재판단 (사고 5 후속) |
| 9/10 09:00 | 검색식 2차 복기 + 동료주 + UX 2건 |
| 12/1 09:00 | 2027년 휴장일 KRX 공지 대조 |

> ⚠️ 리마인더가 새 세션을 깨우면 **그 프롬프트보다 HANDOFF.md가 우선**이다. 먼저 읽고 시작할 것.

---

## 7. 보류 목록 (표본이 더 쌓이면 결정)

- ② **훼이크아웃 검색식 7호** - 프로토타입 49% vs 기준선 28%. 표본 부족 → **9/10에 결정**
- ① 짝꿍 시차 눌림
- 마디가격(라운드 피겨) 지지
- 지지라인 밴드
- S18 리스크 예산 지킴이
- 리포트 레이더 2단계 (목표가 파싱)

---

## 8. 절대 규칙 10개 (요약 - 정본은 CLAUDE.md)

1. `data.json` 스키마 하위호환 유지 (**추가만**, 삭제·개명 금지)
2. 수정 후 `python test_parsers.py` **전체 통과** 필수, 새 기능엔 테스트 추가
3. 휴장일 스킵·품질 가드 우회 금지
4. 한국 관례 색: **빨강=상승, 파랑=하락**
5. 네이버 예의: `REQUEST_DELAY` 제거 금지
6. 시크릿 노출 금지 (환경변수만)
7. 점수 산식은 CONFIG 값으로 조정
8. **비용 0원** (유료 API·LLM API 금지)
9. **엠대시(—) 사용 금지** - 화면·텔레그램·로그 등 사용자가 보는 텍스트. 기획 문서(.md)와 코드 주석은 예외
10. **0픽셀 원칙** - 신규 기능이 상시 화면 길이를 늘리지 않는다 (클릭 확장·조건부 표시만)

### 작업 순서

1. `data.json`의 `errors` 배열 확인 (0번)
2. 코드 수정
3. `python test_parsers.py` → 114/114
4. `notify.py`를 건드렸으면 **`--dry-run`으로 아침·저녁·주간 3종 미리보기**
5. `grep -c "—"` 로 엠대시 확인
6. ZIP으로 전달 → 사용자 업로드 → **별도 클론에서 `git pull` 후 `cmp -s`로 검수**

---

## 9. AI 조회 함정 (HANDOFF 6절 요약)

- `raw.githubusercontent` 캐시 - 방금 올린 파일이 옛 내용으로 보일 수 있다
- Pages 캐시 - 화면 반영이 늦다
- **큰 파일 잘림** - `index.html`(약 141KB)은 조회 시 뒤가 잘린다. 이번 세션에서도 재현됐다. "없다"고 단정하기 전에 잘림 여부부터 확인
- 부정확한 개수 세기 - 눈으로 세지 말고 `grep -c`

---

## 10. 테스트 파일 주의

`test_parsers.py`의 러너는 `if __name__ == "__main__":` 시점의 `globals()`에서 `test_` 함수를 수집한다.
→ **새 테스트는 반드시 그 블록 위쪽에 넣어야 한다.** 아래에 넣으면 조용히 실행되지 않는다.
