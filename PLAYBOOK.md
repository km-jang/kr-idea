# 장애 대응 플레이북 (PLAYBOOK.md)

시스템이 멈추거나 이상할 때 이 문서 하나로 진단→복구한다.
AI에게 맡길 땐 "PLAYBOOK.md 보고 ○○ 증상 고쳐줘"라고 하면 된다.

## 0. 만능 진단 순서 (어떤 증상이든 여기부터)

1. `github.com/km-jang/kr-idea/actions` → 최근 실행이 초록인지 빨강인지
2. 빨강이면: 실행 클릭 → `update` → 빨간 단계 클릭 → 에러 로그 확인
3. 초록인데 이상하면: 각 단계 로그의 마지막 줄 확인 (스킵/생략 문구가 단서)
4. 대시보드 상단의 "장 마감 기준" 날짜 확인 — 며칠 전이면 수집이 안 돌고 있는 것
5. 대시보드 **신호등을 클릭**해 실행 성적표(최근 5영업일 매트릭스) 확인 —
   어느 날 어느 단계가 빠졌는지 한눈에 보인다

## 1. 대시보드가 며칠째 안 바뀜

- **Actions 실행 자체가 없음** → GitHub는 저장소에 60일간 활동이 없으면 예약 실행을 끈다.
  Actions 탭에 노란 배너의 `Enable workflow` 버튼을 누르면 재개. (평소엔 매일 커밋이 생겨 해당 없음)
- **실행은 있는데 빨강** → 아래 2번.
- **초록인데 "휴장일/중복 실행 감지"만 반복** → 연휴면 정상. 평일에도 계속 그러면
  지수 API가 옛 날짜를 주는 것 → AI에게 `fetch_indices`/`is_holiday_rerun` 점검 요청.

## 2. 수집 실패 (빨간 X, "데이터 수집" 단계)

### 로그에 "유니버스 수집 실패"
네이버 목록 API가 바뀐 것. 임시로는 아무것도 안 해도 됨 (직전 데이터 유지).
복구: AI에게 이렇게 요청 —
> "네이버 유니버스 API가 깨졌어. collect.py의 fetch_universe / parse_market_value_item을
> 현재 응답 구조에 맞게 고치고, 폴백(fetch_universe_fallback)도 점검해줘."
대체 소스 우선순위: ① 네이버 데스크톱 페이지(폴백 내장) ② 다음 금융 API ③ KRX 정보데이터시스템(무료 계정 필요)

### 로그에 "수집 품질 미달 - 갱신 중단"
안전장치가 정상 작동한 것 (반쪽 데이터 배포 방지). 하루 이틀 지나도 반복되면
어떤 항목이 미달인지 로그에 나옴 (유니버스/수급/펀더멘털) → 해당 수집 함수 수리 요청:
- 수급 미달 → `fetch_investor_flows` / `parse_frgn_html`
- 펀더멘털 미달 → `fetch_fundamentals` / `parse_integration`

### 업종만 안 나옴 (대시보드 "업종 데이터는 다음 갱신부터")
wiseindex.com 장애/변경. 업종 섹션만 빠지고 나머진 정상 — 급하지 않음.
복구 요청: "fetch_sector_map / parse_sector_items를 점검해줘."

## 2.5 "🚨 자동화 실행 실패" 텔레그램을 받았을 때

시스템이 스스로 보낸 고장 신고다. 메시지의 링크를 열면 해당 실행 로그로 바로 이동
→ 빨간 X가 뜬 단계 이름을 보고 이 문서의 해당 증상 항목으로 이동.
참고: 아침 브리핑은 08:28, 저녁 수집은 19:43에 예비 알람이 한 번 더 시도하므로
실패 통보를 받아도 예비가 성공하면 그날 서비스는 정상일 수 있다.

## 2.6 "🩺 야간 자가 점검" 텔레그램을 받았을 때

야간 점검(21:33)이 보내는 메시지는 두 종류다.

- **"자동 복구했습니다"** → 수집이 늦게 도착해 점검이 대신 수집을 돌린 것. 조치 불요.
  다만 이게 자주 오면 크론 지연이 커지고 있다는 신호다 (아래 참고)
- **"자동 복구도 실패했습니다"** → 진짜 고장일 수 있다. **메시지에 적힌 날짜와 대시보드의
  "장 마감 기준" 날짜를 먼저 비교할 것.** 데이터가 멀쩡한데 이 알림이 왔다면 헛경보다

### 헛경보 이력 (2026-07-27·28 실사고, V4.1에서 수정됨)

크론 지연이 자정을 넘기면 점검이 "오늘"을 다음 날로 인식해서, 아직 장이 서지도 않은 날의
데이터가 없다고 판단하고 복구 실패로 처리했다. 데이터는 멀쩡한데 경보만 울린 것이다.
게다가 실행 성적표가 하루씩 밀려 기록돼 **7/27은 통째로 누락되고 7/28은 정상인 날인데
전부 X로 박제**됐다.

**2026-07-30 수정 완료**: `update.yml`과 `ops_log.py`에 자정 넘김 보정이 들어갔다.
00~06시(KST) 실행이면 기준일을 전날로 되돌린다. 휴장일 판정도 보정된 날짜로 하므로,
금요일 밤 점검이 토요일 새벽으로 밀려도 "주말이니 휴장"으로 버려지지 않는다.

**그래도 헛경보가 온다면**: 보정이 지워졌는지부터 확인할 것.
`update.yml`에 `자정 넘김`, `ops_log.py`에 `base_day`가 있어야 한다.
CLAUDE.md의 "자정 넘김 보정" 항목도 함께 참조.

## 3. 텔레그램이 안 옴

실행이 **초록**인데 안 오면 → "아침 브리핑 발송" 단계 로그 마지막 줄이 답:
- `시크릿이 없어 발송 생략` → Settings → Secrets and variables → Actions의
  **Repository secrets**에 `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` 재등록
  (이름은 영문 대문자+밑줄, 공백 금지. Variables 탭 아님!)
- `브리핑 발송 완료` → 발송은 됨. 텔레그램 앱 알림 설정/차단 확인
- `샘플 데이터 상태 - 발송 생략` → 수집이 한 번도 성공 못한 상태 → 2번 참고

실행이 **빨강**이고 로그에 `403 Forbidden` → 봇 대화방에서 Start를 안 누른 것.
텔레그램에서 내 봇 열고 시작 누른 뒤 재실행. `400 chat not found` → CHAT_ID 숫자 오타.

토큰 유출 의심 시: BotFather에서 `/revoke` → 새 토큰을 시크릿에 교체. 피해 범위는
"봇 명의 메시지 발송"까지이며 계정·자산 접근은 불가능.

### "오프라인 테스트" 단계가 빨강 (`N/M passed`)

테스트가 수집을 차단한 것 (의도된 안전장치). FAIL 난 테스트 이름을 로그에서 찾아
AI에게 전달. 전체 개수는 계속 늘어나므로 **로그에 찍힌 숫자를 기준으로 삼을 것**
(문서에 적힌 개수를 믿지 말 것).
**AI 규칙**: 테스트는 반드시 고정 픽스처만 사용 — 저장소의 실제
data.json/history를 읽는 테스트는 데이터 상태에 따라 터진다 (2026-07-14 실제 장애:
test_compass_message_lines가 data.json을 직접 읽어 구형식 데이터에서 실패 → 수집 차단).
**새 테스트를 추가할 땐 반드시 `if __name__ == "__main__":` 블록 위에 넣을 것** —
러너가 그 시점의 `globals()`에서 `test_` 함수를 모으므로, 파일 맨 끝에 붙이면 실행되지 않는다.

### 낮에 수동 실행(Run workflow)을 했더니 저녁 데이터가 이상함

장중 수동 실행은 그 시점의 '스냅샷'이라 수급(외인·기관)이 미확정 상태다.
2026-07-15 수정 이후로는 저녁 정기 수집이 장중 스냅샷을 자동으로 덮어쓰므로 문제없다.
단, 수동 실행은 가급적 장 마감(15:40) 이후에 할 것.

### 예약 시간이 됐는데 실행 자체가 없음 / 많이 늦음

GitHub 무료 예약의 지연·누락이다. 대책은 이중 알람(예비 크론 4개, update.yml에 내장).
급하면 Actions → 데이터 갱신 → Run workflow로 수동 실행 (수동은 아침 브리핑 형식 발송).

**실측 (2026-07)**: 종가 스캔 2시간 46분 지연(7/13), 저녁 수집 통째로 누락(7/13),
수집 2시간 7분 지연(7/29), 1시간 50분 지연(7/30). 일반적으로 보고되는 5~40분의 서너 배다.
**이 지연 때문에 14:48 종가 스캔과 12:06 점심 맥박이 사실상 제 시간을 놓치고 있다.**
근본 해결안(외부 스케줄러로 정시화 등)은 HANDOFF.md 6절과 ROADMAP.md 14번 참조.

## 4. 사이트가 안 열림 (404/빈 화면)

- 404 → Settings → Pages에서 Branch가 `main` + `/ (root)`인지 확인 후 Save
- 열리는데 "data.json을 불러오지 못했습니다" → data.json이 지워졌거나 손상
  → `history/` 폴더의 최신 날짜 JSON 내용을 data.json으로 복사하면 즉시 복구
- 옛날 화면이 계속 보임 → PWA 캐시. 새로고침 2회 또는 홈 화면 앱 삭제 후 재추가
- **파일을 고쳤는데 `raw.githubusercontent.com`에 옛 내용이 보임** → CDN 캐시.
  URL 뒤에 `?v=2` 같은 아무 쿼리를 붙여 다시 받으면 최신이 온다
  (2026-07-30 실제로 13일 전 파일이 조회된 사례)

## 5. GitHub 자체가 문제일 때 (정책 변경·계정 문제)

이 저장소가 곧 전체 백업이다: 코드 + 일별 히스토리 전부.
- 가벼운 보험: 가끔 `Code → Download ZIP`으로 내려받아 보관
- 이전이 필요하면 (무료 대안): **Cloudflare Pages**(호스팅+이메일 로그인 비공개 가능),
  cron은 cron-job.org 또는 Cloudflare Workers. AI에게:
  > "PLAYBOOK 5번. 이 저장소를 Cloudflare Pages + 외부 cron 구조로 이전하는
  > 마이그레이션을 만들어줘." — 코드 대부분 재사용 가능

## 6. AI에게 수리 맡기는 법 (모델 무관 공통 절차)

1. 새 세션에서 저장소를 연결하거나, 안 되면 `Code → Download ZIP`을 첨부
2. Actions의 에러 로그 몇 줄을 복사해서 함께 전달
3. "CLAUDE.md와 PLAYBOOK.md 읽고 시작해줘. 증상: ○○○" 라고 요청
4. AI가 준 수정 파일을 업로드하기 전, **테스트가 전부 통과했는지 물어볼 것**
   (`N/M passed`에서 N과 M이 같아야 한다)
5. 반영 후 `Run workflow`로 즉시 검증

**파일 전달이 막힐 때**: ZIP 다운로드나 `.py` 파일 다운로드가 브라우저·백신에 막히는 경우가 있다.
그때는 AI에게 **"파일 내용을 채팅에 코드블록으로 붙여줘"** 라고 요청하고,
GitHub 웹 편집기(`github.com/km-jang/kr-idea/edit/main/<파일명>`)에 붙여넣으면 된다.
새 파일은 `Add file → Create new file`. `.github/` 안의 파일은 드래그 업로드가 안 되므로
항상 이 방법을 쓴다.

## 7. 정기 점검 + 백업 (월 1회 · 매월 첫 일요일 주간 결산이 알려줌)

**점검 (1분)**
- Actions 최근 실행들이 초록인지 훑어보기
- 대시보드 "장 마감 기준" 날짜가 최신 영업일인지
- 신호등 클릭 → 실행 성적표에 X가 몰린 날이 없는지
- 성과 트래킹 승률 확인 → 필요하면 CONFIG 튜닝 (README 가이드)

**백업 (2분) — 데이터는 두 곳에 나뉘어 살고, 각자 따로 백업해야 한다**

| 무엇 | 어디 있나 | 잃는 경우 | 백업 방법 |
|---|---|---|---|
| 시스템 전체 (코드+누적 데이터+기록) | GitHub 저장소 | 계정 분실·저장소 삭제 | 저장소 페이지 → 초록 **Code** 버튼 → **Download ZIP** → 컴퓨터/클라우드에 보관 |
| 포트폴리오·관심종목·메모 | 내 폰/PC 브라우저에만 | 기기 교체·분실, 브라우저 데이터 삭제 | 대시보드 포트폴리오의 **백업(CSV)** 버튼 (복원은 옆의 복원 버튼) |

- 매월 첫 일요일 주간 결산 텔레그램에 "💾 백업의 날" 알림이 자동 포함된다
- 포트폴리오 백업을 30일 이상 안 하면 대시보드 리스크 진단에 리마인더가 뜬다
- **기기 바꾸기 전엔 반드시 CSV 백업 먼저!** (복원: 새 기기에서 복원 버튼 → CSV 선택)
