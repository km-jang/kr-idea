# 새 세션 인수인계 요약 (2026-09-01 작성)

> 이 문서는 그날 세션의 요약본이다. **정본은 항상 `HANDOFF.md`** 이고,
> 둘이 어긋나면 HANDOFF를 믿는다. 전날 요약본은 `SESSION-HANDOVER-260831.md`.

---

## 0. 새 세션 첫 마디 (복붙용)

> "km-jang/kr-idea 저장소의 국내주식 대시보드를 이어서 작업할 거야.
> HANDOFF.md → CLAUDE.md → PLAYBOOK.md → SESSION-HANDOVER-260901.md 순서로 읽고 시작해줘.
> **CLAUDE.md 규칙 11과 HANDOFF 10절(결정 기록)은 특히 전문으로 읽어줘.**
> 나는 비개발자니까 모든 안내는 클릭 단위로 쉽게 해줘."

---

## 1. 오늘 무엇이 바뀌었나 (V6.1a → V6.2 "정시")

크론 지연(사고 5)을 5일 겪고 나서, **지연을 견디는 방식에서 지연을 없애는 방식으로** 전환했다.

### ① 정시 실행 (A안)

외부 스케줄러 **cron-job.org**가 GitHub API로 `workflow_dispatch`를 호출한다.
`update.yml`에 `job` 입력을 열어 어떤 작업을 실행할지 지정한다.

| 작업 | 시각 (KST) | Crontab | Request body |
|---|---|---|---|
| morning | 08:03 | `3 8 * * 1-5` | `{"ref":"main","inputs":{"job":"morning"}}` |
| pulse | 12:06 | `6 12 * * 1-5` | `{"ref":"main","inputs":{"job":"pulse"}}` |
| scan | 14:48 | `48 14 * * 1-5` | `{"ref":"main","inputs":{"job":"scan"}}` |
| evening | 19:13 | `13 19 * * 1-5` | `{"ref":"main","inputs":{"job":"evening"}}` |
| night | 23:03 | `3 23 * * 1-5` | `{"ref":"main","inputs":{"job":"night"}}` |

**실전 확인**: 9/1 12:06 점심 맥박이 8/27 이후 처음으로 정시 도착.

### ② 배포 안전벨트 `smoke.yml` (C안, 신규 파일)

`.py`·워크플로가 main에 올라갈 때마다 자동으로 돈다. 결과는 텔레그램으로 온다.

| 점검 | 무엇을 잡나 |
|---|---|
| 0 | 전체 `.py` 문법 (사고 2 계열) |
| 1 | 단위 테스트 114개 |
| 2 | **발송 진입점 3종 dry-run** ← 사고 6이 잡히는 자리 |
| 3 | 스캔·맥박 메시지 조립 (시각 고정으로 창 가드 우회) |
| 4 | 실행 성적표 기록 로직 |
| 5 | 날짜 계산 일치 (**지금은 일부러 실패**. B안 리마인더) |

**검증**: 사고 6 배포본 `f9c63ba`를 이 검사에 넣으면 0·1은 통과하고 **2에서 3종 모두 종료코드 1로 차단**된다.
사고 6의 재발 방지책이 "사람이 기억해서 `--dry-run` 돌리기"였는데 그게 안 지켜져서 자동 게이트로 승격한 것이다.

> `update.yml`과 **일부러 파일을 나눴다.** 합치면 점검이 `update-data` 실행 자리를 차지해
> 마침 그때 도착한 수집·발송이 밀리거나 취소될 수 있다. **합치지 말 것.**

### ③ 무단 실행 감지 (`update.yml` 첫 단계)

외부 호출 시각이 그 작업의 예상 창 밖이면 즉시 텔레그램 경고. 막지 않고 알리기만 한다.
대응 절차는 `PLAYBOOK.md` 2.9절.

### ④ 부수 교정

`sent_log.json`의 `evening`이 자정 넘김 실행 탓에 `2026-09-01`로 잘못 찍혀 있던 것을
`2026-08-31`로 되돌렸다. 안 고쳤으면 9/1 밤 야간 안전망이 "이미 보냈다"고 건너뛸 상태였다.

---

## 2. 오늘 난 사고 2건

### 사고 7 - 장중 시황 3번 발송
외부 스케줄러를 붙이면서 GitHub 예약 10개를 그대로 뒀다. 발사 버튼이 두 개가 됐다.
→ **본 작업 4개의 GitHub 예약을 제거**해서 해결. 예비 4개는 중복 방지 가드가 있어 유지.

### 사고 8 - AI가 결정 기록을 안 읽고 금지된 안을 제안
10절에 "외부 스케줄러 정시화: 보류. 다시 꺼내지 말 것"이 서비스 이름까지 적혀 있었는데
AI가 안 읽고 그대로 제안했다. → **CLAUDE.md 규칙 11** 신설.

둘 다 HANDOFF 8절에 상세히 있다.

---

## 3. 다음 세션 최우선 - B안 (날짜 계산 통일)

**오늘 찾은 근본 원인이고, 아직 안 고쳤다.**

코드 20군데가 "오늘이 며칠인지"를 제각각 계산한다. 규칙이 두 가지로 갈린다.

| 자리 | 자정 넘김 보정 |
|---|---|
| `ops_log.base_day()` · `closing_scan` · 워크플로 셸 2곳 | 있음 (06시 전 = 전날) |
| **`notify.kst_today()`** | **없음** |
| **`collect.py`의 `generated_at`** | **없음** |
| **워크플로 셸 1곳 (15:07 예비 스캔)** | **없음** |

### 실측 피해 (전부 8/31~9/1에 실제로 일어난 것)

1. 8/31 저녁 발송이 **성공**했는데 성적표에 `evening: false`
2. 8/31 마감 데이터가 **정상**인데 `data_final: false` → `recover: fail` → 헛경보 발송 가능
3. 9/1 아침 자가복구가 위 오판 때문에 **장중 10:47에 재수집** → 아침 브리핑이 장중 데이터로 만들어짐

### 고치는 방향

기준일을 **워크플로가 맨 처음 한 번 정해서 모든 스크립트에 인자로 넘긴다.**
스크립트 안에서 `datetime.now()`로 날짜를 다시 만들지 않는다.
그러면 몇 시간이 밀리든 전부 같은 날을 보게 되어 지연이 정확성에 영향을 못 준다.

**`smoke.yml` 점검 5가 이 불일치를 배포마다 경고로 띄운다.** 고치면 통과하고,
그때 그 단계의 `continue-on-error: true` 줄을 지우면 이후 재발이 차단된다.

---

## 4. 반드시 알아야 할 함정

- **트리거가 두 곳이다.** 본 작업 5개 = cron-job.org, 예비·주간·야간 = GitHub 예약.
  여기를 잘못 건드리면 브리핑이 두 번 나가거나 아예 안 온다 (사고 7). HANDOFF 4절 표 참조
- **재발송은 커밋을 안 남긴다.** `sent_log`가 이미 그날 날짜면 파일이 안 바뀐다.
  **커밋 수로 발송 횟수를 셀 수 없다.** 중복 의심은 텔레그램으로만 확인 가능
- **맥박·스캔은 시간 창 가드가 있다** (15:00 / 16:30). 늦으면 안 오는 게 정상이다
- **토큰 권한을 넓히지 말 것.** fine-grained · 저장소 1개 · `Actions` 쓰기만.
  classic `repo` 토큰은 탈취 시 전 시크릿 유출로 번진다 (HANDOFF 10절 채택 조건)

---

## 5. 남은 일

| 항목 | 상태 |
|---|---|
| B안 (날짜 계산 통일) | **미착수. 최우선** |
| 저장소 루트의 중복 `update.yml` 삭제 | 미처리 (동작 무해, 헷갈림 방지용) |
| 토큰을 fine-grained로 교체 | 소유자 작업 대기 |
| 예전 fine-grained 토큰 삭제 | 소유자 작업 대기 |
| 9/4 09:00 크론 지연 재판단 | 정시화로 사실상 해소. 재판단 시 이 문서 참조 |

---

## 6. 오늘의 관측치 (판단 근거로 쓸 것)

- GitHub 예약 지연: 9/1 실측 **약 3시간** (08:03 예정 → 11:04 도착).
  8/27~8/31의 5~14시간에서 줄었으나 **변동이 크다.** 이 변동성 때문에 사고 7이 났다
- `data.json`: `market_date` 2026-09-01, `errors` 비어 있음, 종목 700
- 단위 테스트 114/114 통과
