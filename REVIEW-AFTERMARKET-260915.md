# 애프터마켓 오염 실측 보고 (2026-09-15 · 캘린더 "9/14~9/18 저녁 수집 오염 관찰" 항목)

> 결정 기록(HANDOFF 10절 "애프터마켓 대응: 관찰 후 수정")에 따라 **코드는 손대지 않았다.**
> 이 문서는 실측 결과와 선택지만 담는다. 소유자 승인 후 구현한다.

## 1. 결론 한 줄

9/14(애프터마켓 첫날) 19:14 저녁 수집은 **정규장 종가가 아니라 애프터마켓 진행 중 가격**을 담았다.
700종목 중 538종목(77%)이 정규장 종가와 다르고, 급등·급락 ±5% 판정이 35종목에서 뒤바뀌었다.
그리고 **HANDOFF에 적어 둔 본 수단(일봉 종가 채택)은 쓸 수 없다.** 일봉 API의 종가도 애프터마켓 마지막 체결가(20:00)로 바뀌었기 때문이다.

## 2. 실측 (전부 2026-09-15 10:30~10:50 KST, 네이버 공개 API)

삼성전자 9/14 한 종목으로 세 숫자가 어떻게 다른지:

| 시각·출처 | 값 | 뜻 |
|---|---|---|
| 15:30 분봉 (정규장 마감 단일가) | 249,000 | **정규장 종가**. 9/15 다음날 기준가(네이버·다음 모두 "전일 종가" 249,000)와 일치 |
| 19:13~19:14 분봉 | 248,000 | **data.json `price`가 담은 값** (애프터마켓 진행 중) |
| 19:59 분봉 (애프터마켓 마지막) | 248,500 | **일봉 API·수급 trend API·다음 일봉이 "종가"로 보여주는 값** |

전 종목 대조 (오늘 시세 API의 `현재가 - 전일대비` = 9/14 정규장 종가, 700/700 대조 가능):

| 항목 | 결과 |
|---|---|
| `price` ≠ 정규장 종가 | 538 / 700 (77%) |
| 오차 절대값 | 중앙값 0.93% · 평균 1.04% · 최대 4.03% (성호전자) |
| 오차 1% 이상 / 2% 이상 / 3% 이상 | 249 / 59 / 6 종목 |
| ±5% 급등·급락 판정이 뒤바뀐 종목 | 35 |
| 기준선 9/10 (애프터마켓 전) | 10/10 일치 (오염 없음) |

오염된 필드 (5종목 분봉으로 정규장 09:00~15:30과 애프터마켓 16:00~19:14를 나눠 확인):

- `price` · `change_pct`: 전부 애프터마켓 가격 기준 (삼성전자 -4.43%로 기록, 정규장 실제는 -4.05%)
- `volume`: 애프터마켓 거래량 일부 포함 (삼성전자 정규 16.56M, 기록 17.42M)
- `day_low`·`day_high` (V6.3): 5종목 중 3종목이 애프터마켓 저가로 대체됨 (삼성전자 정규 저가 248,500, 기록 247,000)
- `chart_pack[].closes[-1]`: 19:14 시점의 애프터마켓 가격 (일봉 API 당일 행이 실시간 갱신)
- 지수(`indices` KOSPI 6684.37): **정상** (지수는 애프터마켓 반영 안 됨)
- 점심 맥박·14:48 종가 스캔: **정상** (정규장 시간 안에 돎)

## 3. 왜 기존 가드안이 안 되나

HANDOFF 10절의 본 수단은 "현재가 대신 일봉 종가 채택", 보조는 "수집 20:13 이동"이었다.
둘 다 **애프터마켓 마지막 체결가(248,500)**를 얻을 뿐 정규장 종가(249,000)를 얻지 못한다.
네이버 일봉·수급 API와 다음 일봉이 모두 9/14 종가를 248,500으로 바꿔 놓았다.
정규장 종가는 다음날 기준가로만 쓰이고, 일봉 시계열에서는 사라졌다.

## 4. 깨끗한 출처 (검증 완료)

`api.stock.naver.com/chart/domestic/item/{코드}/minute30?startDateTime=YYYYMMDD0900&endDateTime=YYYYMMDD1530`

- 30분봉 14개. 마지막 봉(15:30)의 `currentPrice` = 정규장 종가. **8/8 종목 일치** (대형주 + 코스닥 소형주 포함)
- 봉의 고가 최대 = 정규장 고가, 저가 최소 = 정규장 저가, 거래량 합 = 정규장 거래량 (시간외 종가매매 15:40~16:00 소량 제외)
- 응답 1.5KB 안팎, 종목당 70~350ms. 700종목이면 요청 +700건/일, 시간 +2~4분 (23:03 점검 여유 3시간 50분 안에 충분)
- 키 불필요, 비용 0원 (규칙 8), `REQUEST_DELAY` 유지 (규칙 5)

## 5. 선택지 (소유자 결정)

| 안 | 내용 | 장점 | 단점 |
|---|---|---|---|
| **A. 30분봉 보정 (권고)** | 19:13 수집 그대로 두고, 종목마다 30분봉 1건을 더 받아 `price`·`change_pct`·`volume`·`day_open/high/low`를 정규장 값으로 덮어씀. `chart_pack` 당일 행도 같은 값으로 교체 | 정규장 기준(교과서·다음날 기준가와 일치). 수급 확정치는 지금처럼 19:13에 받음. 스키마 변경 없음(키 추가 없음, 값만 정확해짐) | 요청 +700건/일, 수집 +2~4분 |
| B. 수집 15:43으로 이동 | 애프터마켓 시작(16:00) 전에 수집 | 요청 증가 0 | 수급(외인·기관) 확정치가 그 시각에 있는지 미확인. 오늘 10:34 기준 수급 API에 당일 행이 없었다 = 장중 잠정치가 안 온다. 확인에 하루가 더 필요하고, 없으면 수급 4축이 통째로 빠진다 |
| C. 수집 20:13으로 이동 | 애프터마켓 종료 후 수집 | 요청 증가 0, 네이버 일봉과 같은 기준 | 정규장 종가가 아니라 애프터마켓 마지막 체결가. 다음날 등락률·기준가와 어긋나고 과거 이력(9/11 이전)과 기준이 섞임 |
| D. 그대로 둠 | 관찰 지속 | 없음 | 매일 오차 중앙값 1%, 급등 판정 오류 35종목/일이 성적표·검색식·스윙에 그대로 쌓임 |

A안 세부 (승인 시 구현 범위): `collect.py`에 `fetch_regular_session(codes)` 추가, 저녁 수집 파이프라인에서 `all_stocks` 갱신 뒤 호출, 실패 종목은 기존 값 유지 + `errors`에 한 줄 (부가 기능 원칙). `chart_pack`의 당일 행 종가도 교체. 테스트 픽스처 2개 추가. 9/14·9/15 이력은 소급 불가(하루 이틀 구멍과 같은 처리).
**ETF·ETN은 애프터마켓 거래 대상이 아니므로 오염이 없다.** A안은 전 종목에 같은 절차를 써도 값이 같아 무해하다.

## 6. 오늘(9/15) 19:13 수집도 같은 방식으로 오염된다

승인이 오늘 안에 나도 구현·테스트·업로드는 내일 19:13 전이 현실적이다. 9/14·9/15 두 날은 오차 1% 안팎의 이력으로 남는다.

## 7. 출처

- 애프터마켓 개요 (16:00~20:00 접속매매, ETF·ETN 제외): 파이낸셜뉴스 2026-09-09, https://www.fnnews.com/news/202609091526314083
- 익일 기준가는 정규장 종가: 실측 (네이버·다음 "전일 종가" 249,000 = 15:30 분봉)
