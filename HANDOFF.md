# kridea 인수인계 (현재 V6.0-260824 눌림 · 문서 2026-08-24)

> 새 세션이 이 문서 하나로 전체 맥락을 복원할 수 있도록 쓴 완전판이다.
> 지침은 CLAUDE.md(절대 규칙 10개 포함), 장애 대응은 PLAYBOOK.md, 기획 기록은 ROADMAP.md.

## 0. 새 세션 첫 마디 (사용자 복붙용)

> "km-jang/kr-idea 저장소의 국내주식 대시보드를 이어서 작업할 거야.
> 먼저 CLAUDE.md, HANDOFF.md, PLAYBOOK.md, ROADMAP.md를 읽고 시작해줘.
> 나는 비개발자니까 모든 안내는 클릭 단위로 쉽게 해줘."

## 1. 새 세션 시작 체크리스트 (읽은 직후 순서대로)

0. **data.json의 `errors` 배열을 먼저 볼 것** (2026-08-25 사고 4의 교훈: 부가 기능은
   예외가 나도 수집을 안 막게 설계돼 있어서, 기능이 죽어도 신호등·성적표는 초록이다.
   `errors`에 한 줄 남는 게 유일한 흔적이다)
1. 이 문서의 6절(조회 함정)을 먼저 숙지할 것. 이전 세션 AI가 여기서 세 번 오진했다
2. 대시보드(https://km-jang.github.io/kr-idea/) "장 마감 기준" 날짜가 최근 영업일인지
3. 신호등 클릭 → 실행 성적표 최근 5영업일에 X가 몰린 날이 없는지
4. 9절(캘린더)에서 오늘 날짜가 지난 항목이 있으면 그것부터 사용자에게 제안
5. 코드를 만지기 전, 필요한 파일을 사용자에게 받아 **전체 테스트를 돌릴 환경**을 갖출 것 (11절)

## 2. 프로젝트 한눈에

- **무엇**: 국내 주식(KOSPI/KOSDAQ) 투자 아이디어 대시보드 + 텔레그램 자동 브리핑
- **소유자**: 비개발자(한국어). 모든 안내는 클릭 단위, 전문용어 없이
- **웹**: https://km-jang.github.io/kr-idea/ (GitHub Pages, 공개 저장소 km-jang/kr-idea, main 루트)
- **비용**: 0원 (GitHub 무료 + 무료 쿼터 API만. 유료 API·LLM API 영구 금지 = 절대 규칙 8)
- **구조**: GitHub Actions 크론 10개 → collect.py 수집·채점 → data.json 커밋 →
  Pages 표시 + notify.py 텔레그램. 단일 파일 HTML, 외부 JS 라이브러리 없음
- **크론(KST)**: 평일 08:03 아침(+08:28 예비) / 12:06 점심 맥박(+12:38 예비) /
  14:48 종가 스캔(+15:07 예비) / 19:13 수집+마감요약(+19:43 예비) / 23:03 야간 자가 점검 /
  일요일 18:07 주간 결산

## 3. 버전 이력 (`Vn-yymmdd`, 뒤 6자리 = 배포일. 배포 시 이 표와 CLAUDE.md 표에 한 줄 추가)

| 버전 | 이름 | 내용 |
|---|---|---|
| V1-260710 | 기본 | 초기 대시보드 (수급·밸류·모멘텀·공시 4축) |
| V2-260716 | 스윙 | 5일선 스윙 레이더 + 청산 도우미 |
| V3-260717 | 안정 | 안정화 1·2탄 (자동복구·실행성적표·크론 정비) |
| V4-260724 | 차트 | 차트 카드 + 휴장일 달력 + 엠대시 정리 |
| V4.1-260730 | 시계 | 자정 넘김 보정 (헛경보·성적표 밀림 차단) |
| V4.2-260731 | 여유 | 야간 점검 21:33 → 23:03, 성적표 오기록 복원, 문서 정비 |
| V5-260731 | 다이어트 | 공시 60건 → 10건 + 더보기 (`index.html`의 `DISC_HEAD`로 조절) |
| V5.1-260731 | 단골 | 🔁 배지: 5선·수급·밸류·스윙 중 2곳 이상 동시 등장 (`buildRegulars`) |
| V5.2-260804 | 블랙박스 | 🚨·🩺 실패 알림에 오류 로그 마지막 줄 동봉 + PLAYBOOK 2.7 |
| V5.3-260804 | 종가클릭 | 종가매매 성적 종목명 클릭 → 퀵메뉴 (all_stocks 이름 매칭, 폴백 유지) |
| V5.4-260814 | 토글 | 검색식 ⚙ 켜기/끄기(브라우저 저장) + 성적 표시(`screen_stats`), ETF 5종 공통 제외, 신고가 문앞 기본 꺼짐 |
| V5.5-260814 | 다이어트2 | 수급/밸류 10종목+더보기, 성과 기록 5일+더보기, 심리 기본 접힘, 모의 투자 숨김(`SHOW_PAPER`) |
| V5.6-260820 | 저울 | ⚖️ 종목 비교기(퀵메뉴 2종목 담기, S5) + 🏃 추격 통계(`chase_stats`, S4) + 주간 결산 발송 장부 커밋(update.yml). **같은 날 소수정 3건 포함**: 뉴스 데뷔 종목 클릭(index), 스윙 성적표 종목 클릭(swing, 이름 매칭→네이버 차트), 내부자 빈 목록 문구 정정(insider) |
| V5.7-260821 | 동료 | 상위 요금제 마감 전 밀린 것 앞당기기. 🤝 동료주 발굴기(pair.html 신설, D 그림자 페어 조기 착수, 연산은 브라우저) + 📋 신호 신뢰도 통지표(S17, 푸터·신호등 성적표에서 진입) + **2027년 휴장일 선등록**(12월 하드 데드라인 선처리, test_holidays_2027 추가) + ETF 절충안 확정 반영(수급 스캐너 표·종가 스캔·뉴스 데뷔 제외, 자금 흐름 막대는 유지) + 공시 날짜 "20260813"→"8/13"(discTime) + 소수정: 성적표 문구 21:33→23:03. **같은 날 2차분(검수 후 보완) 포함**: 스윙 누적 통계 `swing_stats` 키(17회차 136표본, 통지표가 누적 표시) + scan_review·swing_review 항목에 `code` 동봉(이름 매칭→코드 직결) + 동료주 우선주 제외 규칙 정밀화(LG→LG화학 같은 그룹주 짝 오제외 버그 수정) + README 현행화(스케줄·파일 표). **같은 날 3차분(실사용 테스트에서 발견) 포함**: ① **ETF 판별 오탐 수정**: BNK금융지주(일반 은행주)가 접두사 "BNK" 때문에 V5.4부터 ETF로 오판·제외되던 버그 → "접두사+공백" 규칙으로 정밀화(유니버스 500종목 전수 검증), 'ETN' 포함 이름도 지수상품 판정(CD금리 ETN 2종이 접두사를 빠져나가던 구멍) ② **스윙 레이더 ETF 제외** (소유자 확정: CD금리 ETN이 변동 없는 우상향이라 스윙 74점을 받는 구조적 잡음 실사례. 심리 히트는 ETF 유지 결정) ③ 동료주 유니버스에서 우선주·ETN 제외(코드 끝자리 0 규칙) + 다크 모드 관찰 링크 색 ④ dividend.html 배당 공시 날짜도 "8/13" 표기 ⑤ 과거 기록 보기 중 통지표 열면 "지난 기록 기준" 안내. **같은 날 4차분 포함**: **유니버스 500→700 확대** (코스피400+코스닥300, 소유자 확정. 10절 결정 기록 참조) + 동료주 하한 자동완화(표본 40일↑이면 50억→30억) |
| V5.8-260821 | 규율 | 상위 요금제 마감 전 마지막 빌드 (소유자 선택). ① **⏱ 킬 스위치** (S16 ①②, 조기 착수: 스윙 136표본 축적으로 표본 사유 해소): collect.py `build_kill_watch` → data.json `kill_watch` 키. 최근 10스냅샷의 스윙 후보 중 시간 무반응(4영업일↑ ±2% 미만)·거래대금 급감(포착일의 40%↓)을 판정, swing.html에 조건부 섹션 + 후보 카드 경고 태그 (보유·관심 종목은 배지). ③재료 소멸은 AI 팩트 스캐너 출력에 "재료 킬 스위치: 유지/소멸" 판정 추가로 해결 (IDEAS S16 설계 그대로). 실데이터 검증: HMM 10일째 +0.5% 무반응 등 정확히 적중 ② **🧪 백테스트 실험실** (backtest.html 신설, ROADMAP 11 조기 착수): 가중치 4축 슬라이더+프리셋 조합의 과거 성과를 브라우저에서 재계산 (5선 선정 규칙·전략 리그 산식 미러링. **검증: 기본형 재계산 +4.1% = 전략 리그 실측 +4.13% 일치**). 과최적화 경고 명시, 표본 30일 미만은 참고용 라벨. 메인 푸터 링크로 진입 |
| V5.9-260824 | 국면 | **마켓스타PRO 벤치마킹** (소유자 제공 자료에서 채택 3건 + 기각 다수, IDEAS "마켓스타PRO 검수 기록" 참조. 핵심 판단: 실시간 경쟁은 구조적 불가·정체성 충돌이라 기각, "국면(돌파 후 조정 대기)" 프레임만 일배치+통계 검증으로 번역). ① **🪜 돌파 국면 추적** `build_phase_track` → `phase_track` 키: 신고가(52주) 첫 진입 종목의 "첫 조정 1~2일차" 관찰 목록 + 돌파 당일 추격 vs 조정 대기 매수 성적 자동 채점 (착수 시점 실측: 추격 17건 승률 88%인데 평균 -0.89% 꼬리 리스크, 조정 매수 6건 🌱. swing.html 조건부 섹션 + 통지표 행) ② **📑 리포트 레이더** (K3 1단계) `fetch_research_reports`/`parse_research_html`/`build_reports` → `reports` 키: 네이버 리서치 종목분석 목록 일배치, 유니버스 종목·최신일만. index 공시 섹션 위 조건부 박스. **주의: 목록 파서는 픽스처 검증만 마친 상태로 첫 실전이 배포 다음 수집이다. 실패 시 errors에 "reports:" 줄이 남고 수집엔 영향 없음. 목표가·의견 파싱(상세 페이지)은 파서 실전 검증 후 2단계** ③ **🚰 수급 물꼬** 검색식 6호 `turnflow`: 5일 -50억↑ 팔던 외인/기관이 당일 +30억↑ 첫 매수 유턴 (성적은 ⚙에 자동 축적, 기본 켬). 부수: 스윙 카드 모바일 가로 넘침(잠복 버그) 수정, 검색식 문구 5종→6종. **같은 날 모바일 다듬기 포함** (소유자 요청: 아이폰 15 프로 393px 기준 전 페이지 정밀 점검): ① 전 8개 HTML에 `word-break:keep-all` (한국어 단어 중간 줄바꿈 전면 해소, 긴 토큰은 break-word 안전핀) ② 전략 리그 차트 끝 라벨 겹침 방지(세로 밀어내기, `raceChart`) ③ 옆 스크롤 표에 "⇠ 표를 옆으로 밀면 더 보여요" 힌트(JS `markScrollables`가 실제 스크롤 생긴 표만, 모바일 전용. index+dividend) ④ 통지표 성적 문구 축약("다음날" 제거, 범례에 기준 명시) ⑤ 슬라이더 프리셋 줄 상태 문구 제 줄로 ⑥ 스윙 카드 %를 제 줄로 ⑦ chart.html 범례 확대(10.5→13.5, 모바일에서 안 읽히던 것) |
| V6.0-260824 | 눌림 | **눌림매매 프레임 검수 후 채택 2종** (소유자 제공 자료. 정석 4단계는 대부분 기보유였고, 변형 4종 중 2개를 골라 구현. 상세는 IDEAS "눌림매매 검수 기록"). ① **🌊 파동 에너지 눌림** `build_wave_pullback` → `wave_pullback` 키: 급등일(+7%↑·대금 300억↑) 거래대금 대비 조정 구간 누적 대금 비율로 눌림의 질을 3등급(거래 실종<30% · 잘 식음<60% · 아직 활발)으로 나누고, 등급별로 "조정 종가 매수 → 다음날" 성적을 히스토리 전체에서 매일 재채점. **착수 시점 실측이 이론대로 단조 배열**: 거래 실종 19건 승률 58%·평균 +1.94% > 잘 식음 83건 47%·-0.47% > 아직 활발 290건 46%·-1.18%. swing.html `waveSec` + 통지표 행 ② **🩸 마감 투매 눌림 스캔** closing_scan `scan_dump_candidates`: 기존 종가 스캔이 "고가 유지 98%↑"(강한 마감)를 뽑는 것의 **정반대** 조건 = 재료 살아있는 주도주(전일 +5%↑ or 52주 고점 90%↑ or 외인 3일↑)가 당일 고가 대비 -6%↓ 밀린 종목. 텔레그램에 블록 추가, scans.json `dumps` 키에 기록 → collect.py `build_dump_stats`가 익일 자동 채점. **근거: 종가 스캔 실제 실행 시각이 15:01~15:04로 전략이 노리는 마감 직전 창과 겹치고, 실시간 시세에 당일 고가가 이미 실려온다** |
| V6.1-260828 | 지각 | **2026-08-27 크론 지연 사고 대응** (사고 5 참조). ① 종가 스캔·점심 맥박에 **시간 창 가드**: 창 밖(스캔 16:30↑·맥박 15:00↑·새벽) 실행이면 조용히 스킵 (`in_window`) ② 스캔 기록에 **자정 넘김 보정** 적용 (`closing_scan.base_day`, ops_log와 같은 규칙) ③ 채점 3함수가 **시간 창 밖 기록을 자동 배제** (`collect.scan_record_usable`) - 장부를 직접 고치지 않고 오염을 무해화하는 방식 ④ 아침·저녁 브리핑에 **지연 발송 머리말** (`notify.late_notice`, 2시간↑ 지연 시) ⑤ 실행 성적표에 **수집시각 열 + 지연 색상 경고** (`genCell`). **표본 영향**: 추격 통계 47→28건 (7~8월 초 기록 다수가 17시대 = 장 마감 후였음). 성적은 +0.57%→+0.85%로 오히려 개선돼 **부풀림은 없었고 방법론 결함만 제거된 것으로 확인** ← **현재 배포판** |

## 4. 시스템 현재 상태 (2026-08-21 확인)

**완전 정상.** 8/5~8/19 전 영업일 무결점 사이클 (아침·점심·스캔·저녁 전부 발송, recover none).
V5.4의 검색식 성적 자동 집계(screen_stats)와 watchlist 25종목(1군 15 텔레그램 + 2군 10
차트·청산용, 2026-08-14 등록)이 실전 가동 중. 꺼둔 신고가 문앞도 성적은 계속 쌓인다.

- 성적표(ops.json): 7/17~8/13. **7/31·8/3 두 건의 fail은 진짜 장애의 정직한 기록이니 둘 것** (8절)
- **8/6 성적표 행 누락**: 그날 밤 23:03 점검 크론이 통째로 안 돌았다 (GitHub 무료 예약 누락,
  7/13에도 있던 현상). 8/6 데이터·발송은 전부 정상이었으므로 기록만 빠진 것. 조치 불요,
  복원도 하지 않기로 함 (ops.json 직접 수정 금지 원칙)
- 히스토리 구멍: 7/31·8/3 (소급 불가, 수용됨)
- 수집 지연 실측: 평상 약 2시간이나 8/7은 53분, 8/13은 57분, 8/19는 32분으로 변동 큼. 23:03 점검 여유 3시간 50분 유지
- **크론 지연 재실측 완료 (2026-08-21, 캘린더 "8월 말" 항목)**: 최근 5영업일(8/12~8/20) 저녁
  수집 완료가 19:58~20:33, 즉 지연 45분~1시간 20분. 8월 초의 2시간 20분대에서 크게 개선.
  상시서버 논의 불요, 현행 유지. 다음 재실측은 지연이 다시 3시간에 근접할 때만
- 심리 시그널: **작동 중 확인** (sentiment_enabled true, 네이버 키 등록돼 있음. ROADMAP 대기 항목 해소)
- ~~① 추격 통계 화면 표시 확인~~ → **확인 완료** (2026-08-21, chase_stats가 data.json에 실림:
  표본 40건 · 승률 22/40 · 평균 +1.95%. 통지표에도 자동 반영)
- **첫 실전 대기 중인 것 (다음 세션이 확인할 것)**: 주간 결산 발송 장부(sent_log의 weekly)는
  8/23(일) 18:07 발송이 첫 기록. 이게 커밋되면 주간 결산의 감시 사각지대가 공식적으로 사라진다

## 5. 배포 방법 (전 세션들이 실사고로 확립한 절차. 어기면 시스템이 선다)

1. AI 세션은 git push 불가·저장소 쓰기 권한 없음. 파일을 사용자에게 전달 → 사용자가 GitHub 웹에서 반영
2. **코드 파일(.py·.html·.yml)은 조각 붙여넣기 절대 금지. 전체 파일 교체만.**
   (2026-07-31 실사고: 조각 삽입에서 줄바꿈 1개 증발 → SyntaxError → 수집 이틀 전면 중단)
3. 경로별 방법: 일반 파일 = `Add file → Upload files` 드래그 / `.github/` 안 파일 = 드래그 불가,
   웹 편집기(`github.com/km-jang/kr-idea/edit/main/<경로>`)에서 Ctrl+A → Delete → 붙여넣기
4. 사용자 환경에서 채팅 파일 다운로드가 종종 실패한다(특히 .py). 실패하면 **파일 내용을
   채팅 코드블록으로 주고 웹 편집기 전체 교체**로 안내 (이때도 전체 교체 원칙)
5. 업로드 후 화면 맨 아래 **초록 `Commit changes`까지 눌러야 반영**된다 (안 누르고 닫은 사례 있음)
5-1. **파일명 함정 (2026-08-20 실사고)**: 다운로드 폴더에 같은 이름의 예전 파일이 있으면
   브라우저가 새 파일을 `index_1.html`처럼 개명한다. 그걸 드래그하면 교체가 아니라
   **새 파일로 추가**돼 사이트는 옛 파일을 계속 쓴다. 예방: 받기 전에 예전 파일 삭제
   또는 새 폴더에 압축 풀기. 수습: 잘못 올라간 파일과 옛 파일을 지우고 다시 업로드.
   반영 검증 때 `git log`에 낯선 파일명(…_1 등)이 없는지 볼 것
6. 전달 전 필수: 전체 테스트 통과(11절) + UI면 라이트/다크/모바일390px 렌더 + 콘솔 에러 0
7. 반영 검증은 6절의 함정을 피해서 할 것

## 6. ⚠️ AI 조회 함정 4종 (이전 세션이 실제로 당한 것들. 오진의 3대 원인)

1. **큰 파일 뒷부분 잘림**: 웹 조회 도구가 data.json·history·index.html 뒷부분을 자른다.
   `all_stocks`(전 종목 일별 종가·등락률·거래량, 파일 뒤쪽)가 "없는 것처럼" 보인다.
   스키마 판단은 반드시 `collect.py`의 `build_payload()` 코드로 할 것 (한 번 오진했음)
2. **raw.githubusercontent 캐시**: 최대 십수 일 묵은 파일이 온다. URL 뒤 `?v=2` 같은
   쿼리로 재조회 (7/30 실측: 13일 전 파일이 조회됨)
3. **km-jang.github.io(Pages) 캐시**: 쿼리를 붙여도 반나절 묵은 JSON이 온다
   (2026-08-05 실측: 전날 밤 갱신이 안 보여 "밤 사이클 실패"로 오진할 뻔.
   **raw 쪽과 교차 확인**해서 최신인 쪽을 믿을 것)
4. **조회 도구의 개수 세기 부정확**: "엠대시 3개" 같은 카운트는 틀릴 수 있다.
   개수·유무가 중요하면 정확한 문자열 인용을 요구해 눈으로 확인할 것

## 7. 절대 규칙 요지 (전문은 CLAUDE.md. 10개 전부 실사고 또는 소유자 확정 방침)

1 스키마 하위호환(키 추가만) · 2 수정 후 전체 테스트 통과(새 기능엔 테스트 추가) ·
3 휴장일 스킵·품질 가드 우회 금지 · 4 빨강=상승, 파랑=하락 · 5 네이버 예의(REQUEST_DELAY 0.12s) ·
6 시크릿 노출 금지 · 7 점수 산식은 CONFIG로만 · 8 API 비용 0원(유료·LLM 영구 금지) ·
9 엠대시(—) 금지: 화면·텔레그램·Actions 로그 문구 (md 문서·코드 주석은 예외) ·
10 0픽셀 원칙(상시 화면 늘리지 않기, 클릭 확장·조건부 표시만)

## 8. 사고 이력과 교훈 (같은 실수 반복 금지)

### 사고 1: 헛경보 (7/27~28, V4.1로 해결)
크론 지연이 자정을 넘기면 자가점검이 "오늘"을 다음 날로 인식 → 멀쩡한데 🩺 복구실패 발송 +
성적표 날짜 밀림. **수정**: 00~06시(KST) 실행 시 기준일을 전날로 보정 (update.yml 2곳 +
ops_log.py `base_day()`). 휴장일 판정도 보정된 날짜로. **함정**: 시각은 `date '+%-H'`로 뽑아야
한다. `%H`면 08·09가 8진수로 해석돼 bash가 터진다.

### 사고 2: 붙여넣기 SyntaxError (7/31~8/3, 개장일 이틀 수집 중단)
테스트 조각을 웹 편집기에 붙여넣다 마지막 줄바꿈이 증발, `...is None`과 `if __name__...`이
한 줄로 붙어 SyntaxError → 규칙 2의 테스트 게이트가 설계대로 수집을 차단.
**교훈 A**: "모듈이 실전에서 잘 돈다 = 그 모듈의 테스트 파일이 멀쩡하다"가 아니다.
전 세션 AI가 이 잘못된 추론으로 테스트를 용의선상에서 지웠다가 하루를 낭비했다.
**교훈 B**: → 5절 2번(전체 파일 교체 원칙)으로 규칙화.
**대응**: V5.2 블랙박스 — 이제 실패 시 🚨·🩺 텔레그램에 [오류 마지막 줄]이 실려 온다.
사용자가 그 줄을 복사해 주면 즉시 진단 가능 (PLAYBOOK 2.7).

### 사고 5: GitHub 크론 하루 전면 지연 (2026-08-27, V6.1로 대응)
평소 30분~1.6시간이던 예약 지연이 8/27 하루만 **5.4~11시간**으로 폭증했다
(아침 08:03→13:29 · 점심 12:06→23:05 · 스캔 14:48→익일 01:49 · 수집 19:13→익일 05:52).
23:03 야간 점검이 "그날 수집 없음"을 정확히 잡아 알림을 보냈고, 그게 소유자가 받은 메시지다.
- **원인은 우리 코드가 아니다**: 가벼운 아침 브리핑도 5.4시간 밀렸다. 무거운 수집만
  밀렸다면 유니버스 700 확대(8/21)를 의심했겠지만, 8/21~8/26은 전부 정상이었다
- **자정 넘김 보정(V4.1)은 제대로 작동**해 market_date는 8/27로 올바르게 찍혔다.
  다만 그 보정이 update.yml·ops_log에만 있고 **closing_scan에는 없어서** 01:49 스캔이
  '2026-08-28'로 잘못 라벨링됐다 (실제 가격은 5/5건 모두 8/27 종가와 일치 = 하루 밀림)
- **더 나쁜 문제**: 장 마감 후 실행된 스캔은 확정 종가를 보고 후보를 뽑은 셈이라
  (look-ahead) 성적 통계에 섞이면 안 된다. 7~8월 초 기록 다수가 이 상태였다
- **대응**: 시간 창 가드로 애초에 안 돌게 + 채점 단계에서 창 밖 기록 배제.
  **오염 기록을 장부에서 지우지 않고 코드가 무시하는 방식을 택했다** (ops.json 직접 수정
  금지 원칙과 같은 취지 · 앞으로 같은 일이 또 나도 자동으로 걸러진다)
- **교훈**: 지연은 "고장"이 아니라 "지각"이다. 성적표만 보면 둘을 구분할 수 없어
  수집시각 열을 추가했다. 크론 지연이 상시 3시간을 넘기 시작하면 그때는
  10절의 상시서버(Tier2) 논의를 소유자가 다시 꺼낼 시점이다

### 사고 4: 가격 0 불량 레코드가 기능을 통째로 죽임 (2026-08-25, V6.0 핫픽스로 해결)
유니버스 700 확대(8/21) 이후 수집 실패한 ETN 몇 종이 **price 0 · 등락률 -100%**로
data.json에 실려 들어왔다(원본 소스 결함). `build_phase_track`이 그 값으로 나눗셈을 하다
ZeroDivisionError → **돌파 국면 추적 기능이 통째로 사라진 채 하루 넘게 운영됐다**.
- **발견 경위**: V6.0 커밋 검수 중 data.json의 `errors`에 `phase_track: float division by zero`
  한 줄이 남아 있는 것을 보고 추적. 부가 기능은 try로 감싸 수집을 막지 않게 설계했는데,
  그 설계 덕에 시스템은 멀쩡했지만 **동시에 기능 사망이 조용히 묻혔다**
- **교훈**: 부가 기능의 예외는 `errors`에만 쌓이고 아무도 안 본다. **수집 후 data.json의
  errors 배열을 확인하는 것을 검수 루틴에 포함할 것** (신호등·성적표는 이걸 안 본다)
- **조치 3중**: ① 수집 단계에서 가격 0 레코드 제거(근본) ② phase_track·wave_pullback의
  일별 맵 구성에서 배제 + 나눗셈 앞 truthy 가드(증상) ③ backtest.html도 같은 결함이 있어
  `price == null` → `!price`로 수정 ④ 회귀 테스트 `test_zero_price_records_are_ignored` 추가

### 사고 3: ops.json 오염과 복원 (V4.2)
사고 1의 여파로 7/27 기록이 7/28 날짜로 잘못 붙어 있었다(data_gen으로 판별).
날짜를 되돌리고 7/28을 data.json·scans.json 근거로 복원. **ops.json은 직접 수정 금지가
원칙이며, 봇의 버그 기록을 되돌리는 1회성 예외**로만 소유자 승인 하에 진행했다.
(복원 항목 중 morning·pulse·evening은 sent_log가 최신값만 보관해 소급 검증 불가, 추정 복원임)

## 9. 캘린더 (시점이 오면 바로 착수. 지난 항목이 있으면 먼저 제안할 것)

| 시점 | 할 일 | 비고 |
|---|---|---|
| ~~8/14~~ | ~~CONFIG 튜닝 1차 + 검색식 1차 복기~~ ✅ 완료 (2026-08-14, 아래 "검색식 1차 복기 결과" 참조) | 결론: 가중치 현행 유지 (전략 리그 1위가 기본형 +7.9%). 엠대시 잔여 1건도 제거됨 |
| 8/17(월) | 광복절 대체공휴일 | 할 일 없음. 달력이 조용히 쉬면 정상 |
| ~~8월 말~~ | ~~크론 지연 재실측~~ ✅ 완료 (2026-08-21, 4절 참조. 45분~1시간 20분으로 개선, 현행 유지) | |
| **9월 중순 (9/10경)** | **검색식 2차 복기** (남은 것은 이것뿐. ①동료주·③UX 2건은 2026-08-21 V5.7로 조기 완료) | 표본 부족으로 미룬 종합수급·몰래매집·눌림목 거취 확정 + 신고가 문앞 부활 여부 + 추격 통계 리뷰 + IDEAS S16·S18 착수 검토 (S17은 V5.7로 완료). 성적은 대시보드 ⚙과 📋 통지표에 자동 표시되므로 분석 수작업 불요. 동료주 발굴기 첫 성적 관찰도 이때 함께 |
| 크론 해소 후 | 차트 커버리지 12→65 보류 해제 검토 | data.json 비대화가 지연을 악화시켜 보류 중. 8/21 재실측에서 지연이 1시간 안팎으로 줄었으니 9월 복기 때 해제 검토 가능 |
| **12월 초** | ~~holidays_kr.py에 2027년 휴장일 추가~~ → **선등록 완료 (2026-08-21, V5.7)**. 12월엔 KRX 공식 휴장일정 공지와 **대조 확인만** 하면 된다 | 월력요항 기준으로 미리 넣었다. 특히 5/3 노동절 대체·7/19 제헌절 대체가 KRX 공지에도 있는지 볼 것. 등록된 12/1 예약 리마인더는 그대로 두고 대조용으로 쓰기 |

### 검색식 1차 복기 결과 (2026-08-14 · 19일 표본, 폭락 2일 제외)

| 검색식 | 표시 | 다음날 승률 | 3일 후 평균 | 결정 |
|---|---|---|---|---|
| 신고가 문앞 | 13일·24건 (ETF 8건 오염) | 9/23 | -3.5% | **기본 꺼짐** (ETF 제외 후에도 5/15·-4.6%로 구제 불가. 코드 보존, ⚙로 부활 가능) |
| 빈집털이 | 9일·16건 | 10/15 | +0.4% | 유지 |
| 눌림목 | 2일·3건 | 3/3 | +11.7% | 유지. **8/5의 완화안은 철회** (죽은 게 아니라 7월 험한 장에서 안 떴던 것) |
| 종합 수급 | 9일·14건 | 5/12 | -1.2% | 애매. 9월 2차에서 확정 |
| 몰래 매집 | 2일·3건 | 2/3 | +3.5% | 유지. 9월 2차에서 표본 확정 |

CONFIG 가중치: 전략 리그 5주 결과 기본형 +7.9% 1위 (모멘텀 +4.0, 가치 +0.2, 수급 -3.4)
→ 튜닝 사이클 규칙대로 **현행 유지**가 결론. 억지 조정 금지.

## 10. 결정 기록 (소유자 확정. AI가 먼저 뒤집지 말 것)

- **외부 스케줄러(cron-job.org·Cloudflare 등) 크론 정시화: 보류** (2026-07-31).
  GitHub 토큰을 외부에 저장해야 해서 규칙 6과 충돌. **다시 꺼내지 말 것.**
  재검토는 소유자가 먼저 꺼낼 때만. 정시화가 필요해지면 토큰을 안 맡기는 경로(상시서버)로
- 유료 고도화(Tier1 AI·Tier2 상시서버·Tier3 실시간 KIS): UPGRADE-PLAN 참조, 소유자 판단 대기
- M 마일스톤 번호 폐기 → `Vn` 버전으로 통일 (과거 "M2"가 두 뜻으로 쓰여 혼란)
- 안티-FOMO 정체성: 중기 가치·수급 도구. 당일 테마 로켓은 설계상 안 잡는다
- ~~예약 리마인더: 제안됨, 미결~~ → **운용 중 확정** (2026-08-05~). Claude 예약 작업이
  같은 대화 세션으로 알림을 보내는 방식이며 8/14 1차 복기 때 실제로 작동했다.
  현재 등록: ① 9/10 09:00 검색식 2차 복기+동료주+UX 2건 ② 12/1 09:00 2027년 휴장일 추가.
  리마인더 프롬프트에 그날 할 일의 전체 맥락이 담겨 있으므로 소유자는 "시작해"만 하면 된다.
  **⚠️ 주의 (2026-08-21)**: 두 리마인더의 프롬프트는 V5.7 이전에 쓰인 것이라 낡았다
  (동료주·UX 2건·S17은 이미 완료, 12/1은 "추가"가 아니라 "KRX 공지 대조 확인"이 됐다).
  다른 세션 소속이라 프롬프트 수정이 불가했다. **리마인더를 받으면 프롬프트보다 이 문서(HANDOFF)가
  우선이다** · 9절 캘린더의 현재 상태를 기준으로 작업할 것
- 검색식 다이어트 원칙 (2026-08-14): 성적 나쁜 신호는 코드 삭제가 아니라 **기본 꺼짐+⚙ 토글**로.
  데이터는 계속 쌓여 부활 판단 근거가 된다 (신고가 문앞이 이 상태)
- **ETF 제외 범위 확정 (2026-08-21)**: 제외 = 검색식 5종·종가 스캔·뉴스 데뷔·수급 스캐너 표·
  동료주·**스윙 레이더**. 유지 = 자금 흐름 막대(외인 순매수)·**심리 히트**(레버리지 ETF 검색
  급증은 시장 과열 신호로 참고 가치가 있다는 취지)·배당 도구(우선주·리츠 포함이 합리적)
- **유니버스 500→700 확대 (2026-08-21, 소유자 확정)**: 코스피 300→400, 코스닥 200→300
  (collect.py CONFIG `kospi_n`/`kosdaq_n`). 예상 비용: 수집 시간 +10~20분(23:03 점검 여유
  3시간 50분이라 충분), data.json·history 파일 +40%, 네이버 요청 +40%(REQUEST_DELAY 유지).
  **주의 2가지**: ① 성과 트래킹·전략 리그의 비교 기준이 이날 바뀐다 (2026-07-11의 300→500
  확대 때와 같은 유의점. 성적 해석 시 감안) ② 새 200종목은 오늘부터 데이터가 쌓이기 시작하므로
  동료주·검색식 성적에 실리는 건 3주쯤 뒤. 크론 지연이 다시 2시간을 넘으면 이 확대가
  원인 후보 1번이다 (되돌리려면 CONFIG 숫자 2개만 원복)

## 11. 기술 메모 (코드 만지기 전 필독)

- **전체 테스트 로컬 실행에 필요한 파일**: test_parsers.py + collect.py + notify.py +
  closing_scan.py + ops_log.py + holidays_kr.py + **us_kr_map.json + theme_map.json**
  (마지막 2개를 빼먹으면 test_gap_signals·test_theme_map이 헛실패한다. 2026-08-04 확인,
  전부 갖추면 100/100). 새 세션은 사본이 없으니 사용자에게 받거나 raw에서 확보할 것
- **테스트 러너는 `if __name__` 시점의 `globals()`에서 `test_` 함수를 모은다.**
  새 테스트는 반드시 그 블록 **위에**. 맨 끝에 붙이면 조용히 실행 안 됨
- 테스트는 고정 픽스처만 (실제 data.json 읽는 테스트는 데이터 상태 따라 터짐, 7/14 실사고)
- update.yml: 크론 문자열과 스텝의 `if: github.event.schedule == '...'` 문자열은 **쌍**이다.
  한쪽만 바꾸면 그 시각에 아무 스텝도 안 돈다 (V4.2에서 프로그램으로 쌍 검증했음)
- update.yml의 tee·tail 블랙박스 로직(run_log.txt·recover_log.txt)을 지우지 말 것
- index.html 주요 지점: `DISC_HEAD`(공시 표시 건수), `buildRegulars`/`regularBadge`(단골),
  `scanReview`의 `byName`(종가클릭). 퀵메뉴는 `.qn` + `data-c`/`data-n`만 붙이면 전역 위임으로 작동
- scan_review 항목에는 `code`가 없다(이름 매칭으로 해결 중). collect.py를 만질 일이 생기면
  items에 `"code": c.get("code")` 추가를 검토 (키 추가만이라 규칙 1 무해)
- **V5.4~5.5 주요 지점**: collect.py `is_index_product`/`ETF_NAME_PREFIXES`(검색식 ETF 제외),
  `build_screen_stats`(성적 요약 → data.json `screen_stats` 키). index.html `SCKEY`/`DEF_SCREENS_ON`
  (검색식 토글, gate52만 false), `LIST_HEAD`/`PERF_HEAD`(목록 다이어트), `SHOW_PAPER`(모의 투자,
  true로 바꾸면 부활), `DEFAULT_FOLDED`(기본 접힘 섹션). 오프라인 테스트는 현재 103개
- **버튼 클래스 주의**: 표의 "행 더보기"는 `.rowmore`, "열 더보기"는 `.colmore`로 분리돼 있다.
  makeTable의 열 토글이 `.colmore`를 querySelector로 찾으므로 행 버튼에 colmore 클래스를
  섞으면 서로 가로챈다 (V5.5 구현 중 실제로 겪고 분리한 것)
- 저장소를 git으로 통째로 받으면(clone) 6절 조회 함정 1~3(잘림·raw 캐시·Pages 캐시)을
  전부 우회할 수 있다. 세션 환경이 지원하면 이 방법이 가장 정확하다
- **V5.6 주요 지점**: collect.py `build_chase_stats`(추격 통계 → data.json `chase_stats` 키),
  index.html `CMP_PICK`/`cmpAdd`/`openCompare`(비교기, 버튼은 `data-cmp` 위임),
  update.yml 주간 결산 스텝에 sent_log 커밋 포함(이게 없으면 weekly 기록이 러너에서 증발한다,
  실제로 그랬음). **주의**: 전역 CSS `table{min-width:620px}` 때문에 새 표를 만들 땐
  `min-width:0`을 인라인으로 줘야 모바일에서 안 넘친다 (V5.6 구현 중 실제로 겪음)
- **V5.7 주요 지점**: ① 동료주 = pair.html 단독 페이지 (연산 전부 브라우저. 날짜 목록은
  data.json 것을 쓰지 않고 최근 3개월 평일을 직접 만들어 404는 건너뜀 · data.json의
  kospi_trend는 20일, perf_curve는 60일로 잘려 있어 그걸 쓰면 표본이 영영 안 늘어난다.
  시장 평균 대비 초과 등락률로 시차 상관, 우선주 짝은 이름 접두 규칙으로 제외. 튜닝은 상단 CFG)
  ② 통지표 = index.html `openTrust`/`trustGrade` (진입은 푸터 링크 + 신호등 성적표 하단 링크,
  0픽셀). 전역 CSS `th,td{white-space:nowrap}` 때문에 모달 표의 성적 칸엔
  `white-space:normal`을 인라인으로 줘야 모바일 390px에 다 들어간다 (실제로 겪음)
  ③ ETF 접두사 목록이 이제 **3곳**: collect.py `ETF_NAME_PREFIXES`(원본) ·
  closing_scan.py(복제) · index.html+pair.html `ETF_PREFIXES`(JS 복제). 새 운용사 브랜드가
  생기면 전부 함께 고칠 것 ④ 공시 날짜는 index.html `discTime()`
  ⑤ 2027 휴장일: holidays_kr.py + test_holidays_2027 (12월에 KRX 공지와 대조만)
  ⑥ 2차분: collect.py `build_swing_stats`(스윙 누적 → data.json `swing_stats` 키,
  history의 swing_review를 회차 날짜로 중복 제거해 합산, 통지표가 누적 우선 표시) ·
  scan_review·swing_review의 items에 `code` 동봉(index는 이미 `i.code || byName` 우선이라
  자동 적용, swing.html도 동일 패턴으로 수정) · pair.html `sameCompany`는 우선주
  접미(우·우B·1우·2우B 등)만 제외한다 (그룹주 짝은 동료주의 핵심이라 유지).
  오프라인 테스트는 현재 106개
  ⑦ 3차분: `is_index_product`는 이제 "접두사+공백" + "ETN 포함" 규칙 (4곳 전부 동일:
  collect·closing_scan·index `isEtf`·pair `isEtf`). **BNK금융지주가 리트머스 시험지다:
  ETF 목록을 만질 일이 있으면 이 종목이 일반주로 남는지 테스트로 확인할 것**
  (test_is_index_product에 박아둠). 동료주 유니버스는 추가로 코드 끝자리 '0' 필터
  (우선주 005935·ETN 610063 등 제외). build_swing 리스크 게이트 맨 앞에 ETF 게이트
  ⑧ 동료주 유니버스는 고정 순위(상위 250)가 아니라 **거래대금 50억 하한** 방식 (2026-08-21
  소유자 문의로 실험 후 확정: 하한 없이 420종목으로 늘리면 상위 30짝의 2/3가 저유동성
  우연 상관으로 도배됨. 하한 방식은 오늘 기준 260종목, 장 활황 시 자동 확대·잡음 상시 차단.
  탐색 범위를 더 넓히고 싶으면 pair.html CFG의 min_turnover를 낮추면 되지만 50억 밑은 비추)
- **V5.8 주요 지점**: ① 킬 스위치 = collect.py `build_kill_watch` (튜닝은 CONFIG_SWING의
  `kill_window`/`kill_days`/`kill_flat_pct`/`kill_vol_drop`) → swing.html `killSec`/`KILLMAP`.
  swing.html의 기준 안내 문구에도 4일·2%·40%가 박혀 있으니 CONFIG를 바꾸면 그 문구도 함께
  ② 백테스트 = backtest.html 단독 페이지. **PRESETS와 RULE 상수는 collect.py의
  STRATEGY_PRESETS·pick_ideas_weighted를 복제한 것** · 서버 쪽 산식이나 프리셋을 바꾸면
  backtest.html도 함께 바꿔야 "기본형 = 전략 리그" 일치가 유지된다 (일치 여부가 이 도구의
  신뢰 근거. 배포 후 기본형 누적치를 전략 리그 값과 대조해 확인할 것)
  ③ 히스토리 로더는 pair.html과 같은 방식(3개월 평일 전수 + 404 스킵)이라 한쪽에서 받으면
  브라우저 캐시로 다른 쪽도 빨라진다
- **V5.9 주요 지점**: ① 돌파 국면 = collect.py `CONFIG_PHASE`/`build_phase_track`
  (돌파 판정은 all_stocks의 near_52w_pct ≥ 99.9 첫 진입. 조정 통계는 히스토리 전체를
  매일 다시 채점하므로 표본이 저절로 늘어난다). swing.html `phaseSec` · 통지표 "조정 대기 매수" 행
  ② 리포트 = `parse_research_html`(tr 단위 느슨한 정규식, 픽스처 테스트 있음) ·
  `build_reports`는 "목록의 최신 날짜"를 기준일로 삼는 자기 적응형이라 네이버 날짜 형식이
  바뀌어도 동작. index.html `reportsBox`. 첫 실전 결과를 반드시 확인할 것 (실패 시
  data.json errors에 "reports:" 줄) ③ 물꼬 = CONFIG_SCREENS `turnflow` (라벨 3곳:
  collect·notify·index SCREEN_LABELS 함께 수정해야 함, 이번에 3곳 모두 반영됨)
  ④ 9/10 2차 복기 때 물꼬·조정 통계·리포트 레이더 2단계(목표가)도 함께 리뷰할 것
- **V6.0 주요 지점**: ① 파동 에너지 = collect.py `CONFIG_WAVE`/`wave_band`/`build_wave_pullback`
  (튜닝은 surge_chg·surge_turnover·band_tight·band_mid). 등급 경계를 바꾸면 과거 성적도
  전부 다시 계산되므로 **경계를 바꾼 날은 성적 비교 기준이 끊긴다** (바꿀 거면 9/10 복기처럼
  정해진 시점에)  ② 투매 눌림 = closing_scan.py `CONFIG_DUMP`/`scan_dump_candidates`.
  **본 스캔(scan_candidates)과 조건이 정반대라 둘이 같은 종목을 뽑는 일은 구조적으로 없다.**
  실패해도 본 스캔은 나가도록 try로 감쌌음. 채점은 collect.py `build_dump_stats`
  ③ scans.json에 `dumps` 키가 추가됐다 (키 추가만, 기존 candidates 채점 로직 무영향).
  **첫 실전은 배포 후 첫 종가 스캔이고, 그날 저녁 수집부터 dump_stats가 쌓이기 시작한다**
  ④ 우선주·ETN은 투매 스캔·동료주에서 코드 끝자리 '0' 규칙으로 제외 (본주의 그림자)

## 12. 사용자 액션 대기 (다음 세션이 리마인드할 것)

- ~~watchlist.txt 채우기~~ → **완료** (2026-08-14, 25종목: 한화 그룹 5 + 대형주 3 +
  KOSPI 섹터 대장 4 + KOSDAQ 대장 3 = 1군 15가 텔레그램 대상, 2군 10은 차트·청산 점검만.
  notify가 상위 15만 읽는 구조를 이용한 2군 체제)
- ~~네이버 개발자 키(심리 시그널) 등록 여부 미확인~~ → **등록·작동 확인됨** (2026-08-14)
- ~~DART 키 등록 여부 미확인~~ → **등록·작동 확인됨** (2026-08-14, insider_trades에 실데이터)
- 현재 사용자 액션 대기 항목 없음. 다음 확인 거리는 일요일 주간 결산뿐인데,
  V5.6부터 발송 장부(sent_log의 weekly)로 저장소에서 확인 가능해졌다

## 13. 장애 시

PLAYBOOK.md 증상별 매뉴얼. 진단 순서: Actions 초록/빨강 → 대시보드 날짜 → 신호등 성적표.
🚨·🩺 알림에 [오류 마지막 줄]이 실려 온다(V5.2). 그 줄만 받으면 대부분 즉시 진단된다.
수집 연속 실패 패턴은 PLAYBOOK 2.7. 자정 넘겨 온 🩺는 V4.1에서 고쳤으니 오면 진짜부터 의심.
