# kridea 인수인계 (현재 V4.2-260731 여유 · 문서 2026-07-31)

## 0. 새 세션 첫 마디 (복붙용)

> "km-jang/kr-idea 저장소의 국내주식 대시보드를 이어서 작업할 거야.
> 먼저 CLAUDE.md, HANDOFF.md, PLAYBOOK.md, ROADMAP.md를 읽고 시작해줘.
> 나는 비개발자니까 모든 안내는 클릭 단위로 쉽게 해줘."

## 1. 버전 이름 규칙

`Vn-yymmdd` (숫자=순번, 뒤 6자리=배포된 날짜). 전체 표는 CLAUDE.md의 "버전 이름 규칙" 절에 있다.

| 버전 | 이름 | 내용 |
|---|---|---|
| V1-260710 | 기본 | 초기 대시보드 (수급·밸류·모멘텀·공시 4축) |
| V2-260716 | 스윙 | 5일선 스윙 레이더 + 청산 도우미 |
| V3-260717 | 안정 | 안정화 1·2탄 (자동복구·실행성적표·크론 정비) |
| V4-260724 | 차트 | 차트 카드 + 휴장일 달력 + 엠대시 정리 |
| V4.1-260730 | 시계 | 자정 넘김 보정 (헛경보·성적표 밀림 차단) + 지침 정비 |
| V4.2-260731 | 여유 | 야간 점검 21:33 → 23:03, 성적표 오기록 복원, 문서 4종 정비 ← **현재 배포판** |
| V5-(작업중) | 미정 | (다음) 화면 다이어트 + 단골 배지. 크론 정시화는 보류 (아래 6절) |

## 2. 프로젝트 한눈에

- **무엇**: 국내 주식(KOSPI/KOSDAQ) 투자 아이디어 대시보드 + 텔레그램 자동 브리핑
- **소유자**: 비개발자(한국어). 모든 안내는 클릭 단위로, 전문용어 없이
- **웹**: https://km-jang.github.io/kr-idea/ (GitHub Pages, 공개 저장소 km-jang/kr-idea, main 루트)
- **비용**: 서버비·API비 완전 0원
- **구조**: GitHub Actions(크론 10개) → collect.py 수집·채점 → data.json 커밋 →
  Pages가 대시보드 표시 + notify.py가 텔레그램 발송. 단일 파일 HTML(외부 JS 라이브러리 없음)
- **절대 규칙 10개는 CLAUDE.md에 있다. 작업 전 반드시 읽을 것.**

## 3. 배포 방법 (특수 환경, 실전 확인됨)

AI 세션에서 git push가 막힌다. 그리고 **사용자 환경에서 파일 다운로드가 안 되는 경우가 많다**
(특히 `.py`는 브라우저·백신이 차단). 그래서 가장 확실한 경로는 다음과 같다.

- **파일 내용을 채팅에 코드블록으로 붙여주고, 사용자가 GitHub 웹 편집기에 붙여넣게 한다.**
  기존 파일: `github.com/km-jang/kr-idea/edit/main/<파일명>` → Ctrl+A → Delete → 붙여넣기 → Commit
  새 파일: 저장소 → `Add file` → `Create new file` → 파일명 입력 → 붙여넣기 → Commit
- 다운로드가 되는 경우에만 ZIP 전달 + `Add file → Upload files` 드래그를 쓴다.
  단 `.github/` 안의 파일은 드래그가 안 되므로 항상 웹 편집기로 안내한다.
- 전달 전 반드시: 테스트 전체 통과 + (UI면) 라이트/다크/모바일(390px) + 콘솔에러 0
- 반영 후 `raw.githubusercontent.com`으로 확인. **캐시된 옛 내용이 올 수 있으니 URL 뒤에
  `?v=2` 같은 쿼리를 붙여 다시 받을 것** (2026-07-30 실제로 13일 전 파일이 조회된 사례 있음)

## 4. 파일 지도

CLAUDE.md의 "아키텍처" 표가 최신이다. 핵심만:

| 파일 | 역할 |
|---|---|
| `collect.py` | 수집·채점 파이프라인. 튜닝은 상단 CONFIG 계열만 |
| `notify.py` | 텔레그램 발송 (아침 / --evening / --weekly) |
| `closing_scan.py` | 14:48 종가매매 스캔 + --pulse(점심 맥박) |
| `holidays_kr.py` | KRX 휴장일 달력. `closed_reason(날짜)`로 날짜 인자를 받는다 |
| `ops_log.py` / `ops.json` | 실행 성적표 기록기 / 장부. `base_day()`가 새벽 실행을 전날로 보정 |
| `index.html` | 메인 대시보드 (신호등 클릭 시 실행 성적표, 종목 클릭 시 퀵메뉴) |
| `chart.html` | 차트 카드 (120일 차트 + 판정 4줄). data.json `chart_pack` 사용 |
| `swing.html`·`dividend.html`·`insider.html`·`mine.html` | 하위 페이지 |
| `test_parsers.py` | 오프라인 테스트. 러너가 `globals()`에서 `test_` 접두사를 모으므로 **새 테스트는 반드시 `if __name__` 블록 위에 넣을 것** |
| `.github/workflows/update.yml` | Actions 워크플로 (크론 10개) |

## 5. V4.1에서 고친 것 (2026-07-30)

**헛경보 버그.** 크론 지연이 자정을 넘기면 자가점검이 "오늘"을 다음 날로 인식해서,
아직 장이 안 선 날의 데이터가 없다고 판단하고 복구 실패 처리 후 🩺 헛경보를 보냈다.

- `update.yml`: 자가점검·19:43 예비 두 곳에 **자정 넘김 보정** 추가.
  00~06시(KST) 실행이면 기준일을 전날로 되돌린다. 시각은 `date '+%-H'`로 뽑는다
  (`%H`를 쓰면 `08`·`09`가 8진수로 해석돼 bash가 터진다)
- 휴장일 판정도 보정된 날짜로 한다(`holidays_kr.py "$TODAY"`).
  덕분에 **금요일 밤 점검이 토요일 새벽으로 밀려도 "주말이니 휴장"으로 버려지지 않는다**
- `ops_log.py`: `base_day()` 추가. 날짜를 인자로 받고, 없으면 스스로 보정 판단
- `CLAUDE.md`: 규칙 9(엠대시 금지)·10(0픽셀) 명문화, 버전 이름 규칙 표 추가
- `update.yml`의 엠대시 7곳 제거 (V4에서 `.github/` 안쪽을 놓쳤었다)

**검증**: 7/28 사고 재현 테스트 통과. 7/30 밤 실전에서 정상 통과(`recover: none`).
`test_parsers.py`에 `test_ops_log_base_day` 등 3건 추가.

## 5.1 V4.2에서 고친 것 (2026-07-31)

- **야간 점검 21:33 → 23:03** (`3 14 * * 1-5`). 수집(19:13)과의 간격을 2시간 20분에서
  3시간 50분으로. 스텝의 `if:` 조건 문자열도 함께 바꿔야 동작한다(둘이 어긋나면 그 시각에
  아무 스텝도 안 돈다). **이 간격을 다시 좁히지 말 것**
- **`ops.json` 오기록 복원.** 07-28 행의 `data_gen`이 `2026-07-27 22:22 KST`였다.
  그 행은 7/28이 아니라 **7/27의 기록이 날짜만 잘못 붙은 것**이었다.
  날짜를 7/27로 되돌리고, 누락돼 있던 7/28 행을 `data.json`·`scans.json` 근거로 복원했다.
  ※ `data_final`·`data_gen`·`scan`은 파일로 검증된 값이고, `morning`·`pulse`·`evening`은
  당시 `sent_log.json`이 최신 날짜만 보관해 소급 검증이 불가능하다. 그 이틀 동안
  🚨 실행 실패 통보가 없었다는 점에 근거해 정상으로 복원했다(추정임을 명시해 둔다)
- `ROADMAP.md`·`PLAYBOOK.md` 정비. ROADMAP의 미착수 후보 5·7·8·9번이 이미 완료된
  기능과 중복 기재돼 있던 것을 정리하고, M 번호를 폐기해 `Vn` 버전으로 통일했다

## 6. 미해결 / 다음 세션이 먼저 볼 것

### 🔴 크론 지연이 상시 2시간 (근본 원인)

| 날짜 | 수집 예정 | 실제 완료 | 지연 | 옛 점검(21:33)까지 여유 |
|---|---|---|---|---|
| 2026-07-29 | 19:13 | 21:20 | 2시간 7분 | 13분 |
| 2026-07-30 | 19:13 | 21:03 | 1시간 50분 | 30분 |

일반적으로 보고되는 GitHub Actions 지연은 5~40분인데 이 저장소는 그 서너 배다.
**점검이 수집보다 먼저 도착하면 여전히 오작동한다.** 자정 넘김 보정은 이 순서 역전을
완전히 막지 못한다(다만 결과가 "복구 실패 경보"에서 "복구했습니다 알림"으로 순해졌다).

**V4.2에서 점검을 23:03으로 옮겨 여유를 3시간 50분으로 넓혔다.** 위 실측 범위는 덮지만
근본 해결은 아니다. 지연이 4시간을 넘으면 다시 깨진다.

그리고 이 지연 때문에 **14:48 종가 스캔과 12:06 점심 맥박이 사실상 죽어 있다**
(각각 17시·15시 도착). 기능이 0의 가치를 내는 중. 이건 점검 시각을 옮겨도 안 풀린다.

남은 해결안:
1. ~~점검 시각 21:33 → 23:03~~ ✅ V4.2-260731 적용 완료
2. **외부 무료 스케줄러로 정시화** (0원). cron-job.org 또는 Cloudflare Workers가
   `repository_dispatch`로 워크플로를 직접 깨운다. 지연이 1분 이내로 줄고 죽은 기능 2개가 살아난다.
   **대가: GitHub 토큰을 외부 서비스에 저장**해야 하므로 규칙 6과 부딪힌다.
   **→ 2026-07-31 소유자가 토큰 리스크를 이유로 보류 결정. 다시 꺼내지 말 것.**
   재검토하려면 소유자가 먼저 꺼내야 한다
3. **상시 서버** (오라클 무료 0원 ~ 헤츠너 월 7천원). 토큰을 남에게 맡기지 않는 유일한 정시화 경로.
   관리 부담이 비개발자에게 가장 큰 비용. 유료 고도화 결정과 함께 판단

### 🟡 히스토리 스냅샷의 한계 (9월 동료주 기능의 전제)

`history/*.json`은 유니버스 500종목 전부가 아니라 **그날 선별된 종목(`ideas` 등)만** 담는다.
종목별 항목에 `closes`(종가 배열)가 있어 과거 시계열은 얻을 수 있지만,
**전체 유니버스의 일별 종가·거래대금이 매일 쌓이지는 않는다.**
9월에 그림자 페어(동반 등락 상관)를 하려면 이 커버리지로 충분한지 먼저 확인할 것.
부족하면 스냅샷에 경량 종가 테이블을 추가해야 하고, 그건 빠를수록 좋다.

## 7. V5 후보

크론 정시화가 보류되면서 V5의 얼굴이 화면 개선으로 돌아왔다.

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | 화면 다이어트 (공시 60건 → 10건 + 더보기) | 메인 세로 11,486px. 매일 겪는 불편 |
| 2 | 단골 배지 (반복 등장 종목) | 싸고 0픽셀, 새 데이터 없음 |
| 3 | 히스토리 커버리지 점검 (위 6절) | 9월 기능의 전제. 늦으면 되돌릴 수 없다 |
| 보류 | 차트 커버리지 12 → 65종목 | data.json이 커져 **수집 시간이 늘고 지연이 악화**된다. 크론 지연이 살아 있는 한 위험 |

**거의 공짜인데 안 하면 9월에 후회할 것**: `history/` 스냅샷에 상관분석(동료주)에 필요한
필드(종가·거래대금)가 매일 제대로 쌓이는지 지금 확인. 2개월 쌓은 뒤 없는 걸 발견하면 늦다.

## 8. 대기·보류

- **클릭 → 동료주(테마 페어)**: 업종 분류는 너무 coarse해서 부적합(달바·실리콘투·코스맥스가 안 묶임).
  정답은 동반 등락 상관(그림자 페어). 히스토리 2~3개월 필요, 9월 타깃
- 백테스트 프레임 · 어닝 시그널 · 스윙 셋업별 적중률 점수 승격
- **사용자 액션 대기**: `watchlist.txt`가 비어 있다(주석 예시만).
  채우면 관심종목 알림 + 스윙 청산 + 차트카드 대상이 한꺼번에 늘어난다
- **유료 고도화 결정**: Tier1 AI 분석 / Tier2 상시서버 / Tier3 실시간(KIS API). UPGRADE-PLAN 참조.
  정체성 메모: 이 시스템은 중기 가치·수급 도구라 당일 테마 로켓은 설계상 안 잡는다(안티-FOMO)

## 9. 장애 시

`PLAYBOOK.md`의 증상별 매뉴얼. 핵심 진단 순서: Actions 초록/빨강 → 대시보드 "장 마감 기준" 날짜 →
신호등 클릭해 실행 성적표 매트릭스 확인.
※ 자정 넘겨 온 🩺 복구실패는 V4.1에서 고쳤다. 그래도 온다면 데이터 날짜부터 확인할 것.
