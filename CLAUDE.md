# AI 작업 지침 (CLAUDE.md)

이 파일은 AI 어시스턴트(Claude 등 모든 모델)가 이 저장소에서 작업할 때 따라야 할
지침이다. 사람이 읽어도 유용하다. **작업 전 반드시 이 파일과 README.md를 읽을 것.**

## 프로젝트 개요

국내 주식(KOSPI/KOSDAQ) 투자 아이디어 대시보드. 소유자는 비개발자이므로
**모든 안내는 클릭 단위로 쉽게**, 전문용어 없이 설명한다.

- 웹: https://km-jang.github.io/kr-idea/ (GitHub Pages, main 브랜치 루트)
- 자동화: GitHub Actions (`.github/workflows/update.yml`) · 스케줄 10개
  - 평일 08:03 아침 브리핑(신선도 확인+자가복구, +08:28 예비) / 12:06 점심 맥박 (+12:38 예비)
    / 14:48 종가 스캔 (+15:07 예비) / 19:13 수집+마감요약 (+19:43 예비)
  - 일요일 18:07 주간 결산 · 평일 23:03 🩺 야간 자가 점검(감시 + 자동 복구 + 실행 성적표 기록)
  - **점검을 21:33에서 23:03으로 옮긴 이유 (2026-07-31)**: 수집 지연이 이틀 연속 2시간
    안팎이라 점검이 수집을 추월할 뻔했다(여유 13분·30분). 수집(19:13)과의 간격을
    2시간 20분에서 3시간 50분으로 넓혔다. 이 간격을 다시 좁히지 말 것
  - 크론 분(minute)은 정각·5분 배수를 피해 배치 — GitHub 혼잡 시간대 지연·누락 회피 (2026-07-17)
  - concurrency `cancel-in-progress: false` 유지할 것 — true면 지연된 수집이 다음 크론에
    강제 취소되어 '조용한 유실'이 생긴다 (2026-07-16 실장애). 모든 git push는 실패 시
    pull --rebase 재시도 포함 (커밋 겹침 유실 방지)
  - **자정 넘김 보정 (2026-07-30)**: 크론이 크게 밀려 00~06시(KST)에 실행되면 기준일(`TODAY`)을
    전날로 되돌린다. 이 보정을 지우면 지연될 때마다 "오늘 데이터 없음" 오판 → 🩺 헛경보 발송 +
    실행 성적표가 하루씩 밀려 기록된다 (2026-07-27 기록 누락, 07-28 오점 박제 실사고).
    `ops_log.py`의 `base_day()`가 같은 규칙을 쓰므로 둘을 함께 유지할 것
  - 모든 예비 알람은 본 실행 완료(마감 후 수집 여부·발송 장부·scans 날짜)를 확인하면 아무것도 안 함
  - 어떤 단계든 실패하면 텔레그램 자동 통보 + 결과물이 이상하면 야간 점검이 통보·자동 복구
    (즉 "조용한 실패"까지 감시망에 포함)
  - `sent_log.json` = 발송 장부 (봇이 커밋, 예비 알람의 중복 발송 방지) — 직접 수정 금지
  - `ops.json` = 실행 성적표 장부 (야간 자가 점검이 매일 기록, `ops_log.py`) —
    대시보드 신호등 클릭 시 최근 5영업일 매트릭스 표시. 직접 수정 금지
- 알림: 텔레그램 (Secrets: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, 선택 `DART_API_KEY`)

## 버전 이름 규칙 (프로젝트명 `kridea`)

버전은 `Vn-yymmdd` 형식. 숫자는 순번, 뒤 6자리는 **완성·배포된 날짜**.
새 세션은 "kridea Vn 작업 시작"으로 열고, 배포하는 날 날짜를 붙여 확정한다.
확정된 버전은 아래 표에 한 줄 추가하고 `HANDOFF.md`도 같은 이름으로 갱신한다.

| 버전 | 이름 | 내용 |
|---|---|---|
| V1-260710 | 기본 | 초기 대시보드 (수급·밸류·모멘텀·공시 4축) |
| V2-260716 | 스윙 | 5일선 스윙 레이더 + 청산 도우미 |
| V3-260717 | 안정 | 안정화 1·2탄 (자동복구·실행성적표·크론 정비) |
| V4-260724 | 차트 | 차트 카드 + 휴장일 달력 + 엠대시 정리 |
| V4.1-260730 | 시계 | 자정 넘김 보정 (헛경보·성적표 밀림 차단) + 지침 정비 |
| V4.2-260731 | 여유 | 야간 점검 21:33 → 23:03, 성적표 오기록 복원, 문서 4종 정비 |
| V5-260731 | 다이어트 | 공시 60건 → 10건 + 더보기 (`index.html`의 `DISC_HEAD`로 조절) |
| V5.1-260731 | 단골 | 🔁 단골 배지 (5선·수급·밸류·스윙 중 2곳 이상 동시 등장). 새 데이터 없이 계산 |
| V5.2-260804 | 블랙박스 | 🚨·🩺 실패 알림에 오류 로그 마지막 줄 동봉 (run_log/recover_log tee). PLAYBOOK 2.7절 신설 |
| V5.3-260804 | 종가클릭 | 종가매매 성적 종목명 클릭 → 퀵메뉴. scan_review에 code가 없어 all_stocks 이름 매칭, 실패 시 맨글자 폴백 |
| V5.4-260814 | 토글 | 검색식 ⚙ 켜기/끄기 + 성적 표시 (`screen_stats` 키 추가, `build_screen_stats`). ETF 5종 공통 제외 (`is_index_product`). 신고가 문앞 기본 꺼짐 (19일 표본 승률 39%·3일 후 -3.5%) |
| V5.5-260814 | 다이어트2 | 수급/밸류 10종목+더보기(`LIST_HEAD`), 성과 기록 5일+더보기(`PERF_HEAD`), 심리 기본 접힘, 모의 투자 숨김(`SHOW_PAPER`, 코드·기록 보존) |
| V5.6-260820 | 저울 | ⚖️ 종목 비교기(퀵메뉴, S5) + 🏃 추격 통계(`chase_stats` 키, S4) + 주간 결산 발송 장부 커밋(update.yml, 감시 사각지대 제거) |
| V5.7-260821 | 동료 | 🤝 동료주 발굴기(pair.html, 브라우저 연산) + 📋 신호 신뢰도 통지표(S17, 푸터·성적표 링크) + 2027년 휴장일 선등록 + ETF 절충(수급 표·종가 스캔·뉴스 데뷔 제외, 자금 흐름 막대 유지) + 공시 날짜 "20260813"→"8/13" |
| V5.8-260821 | 규율 | ⏱ 킬 스위치(S16 ①시간 ②대금, `build_kill_watch` → `kill_watch` 키, swing.html 조건부 표시 + AI 스캐너에 재료 킬 스위치 판정) + 🧪 백테스트 실험실(backtest.html, 가중치 조합 재계산 · 전략 리그 산식 미러링, 기본형 실측 일치 검증) |
| V5.9-260824 | 국면 | 마켓스타PRO 벤치마킹 3종 (IDEAS 검수 기록 참조). 🪜 돌파 국면 추적(`build_phase_track` → `phase_track` 키: 신고가 돌파 후 첫 조정 1~2일차 관찰 + 돌파 추격 vs 조정 대기 성적 자동 채점, swing.html·통지표) + 📑 리포트 레이더(`fetch_research_reports` → `reports` 키, 네이버 리서치 목록 일배치·목표가는 2단계) + 🚰 수급 물꼬 검색식 6호(`turnflow`: 5일 순매도 → 당일 대량 순매수 유턴) |
| V6.0-260824 | 눌림 | 눌림매매 프레임 채택 2종 (IDEAS 검수 기록 참조). 🌊 파동 에너지 눌림(`build_wave_pullback` → `wave_pullback` 키: 급등일 대금 대비 조정 구간 대금 비율로 눌림의 질을 3등급화 + 등급별 성적 자동 채점) + 🩸 마감 투매 눌림 스캔(closing_scan `scan_dump_candidates`, 종가 스캔의 역방향 · scans.json `dumps` 키 → `dump_stats`로 익일 채점) |
| V6.1-260828 | 지각 | 크론 지연 사고 대응: 종가 스캔·맥박 시간 창 가드 + 스캔 기록 자정 보정 + 채점 시 창 밖 기록 배제(look-ahead 차단) + 지연 발송 머리말 + 성적표 수집시각 열 |
| V6.1a-260831 | 지각(핫픽스) | notify.py에 `KST` 미정의로 브리핑 발송이 전부 NameError로 실패한 것을 수정 (사고 6). 테스트에 기본 인자 경로 검증 추가 |
| V6.2-260901 | 정시 | 외부 스케줄러로 5개 본 작업 정시화 + 배포 안전벨트 `smoke.yml` 신설 + 무단 실행 감지. GitHub 예약은 본 작업 4개 제거(중복 발송 차단, 사고 7) |
| V6.3-260903 | 구름 | **일목 근사 + 당일 시가·고가·저가 축적** (소유자 승인, IDEAS 검수 기록 참조). ① `ichimoku_state()`: 차트팩 종목의 120일 종가로 구름 위치·두께·후행스팬·26일 뒤 구름 하단을 계산해 `chart_pack[코드].ichimoku`에 6필드 저장 (+4KB). **표시·기록만. 표본 25일 전까지 점수·손절 미반영** ② `fetch_daily_hl()`: 저녁 수집이 장 마감 뒤에 도는 점을 이용해 실시간 API에서 당일 시가·고가·저가를 받아 `all_stocks`에 싣는다 (+23KB). history/에 쌓여 78거래일 뒤 근사를 실측으로 바꾼다. 그동안 '구조적 불가'였던 윗꼬리·도지 판정도 이때 풀린다 ③ 채택 근거(실측): 오늘의 5선 중 3개가 구름 아래였다 = 기존 점수와 겹치지 않는 새 정보. 반대로 구름 하단을 손절선으로 쓰라는 원안은 손절폭 중앙값 -19.5%로 나와 기각 |
| V6.4-260908 | 당직 | **무발송 감시** (소유자 지시). cron-job.org(정시 스케줄러)나 그 토큰이 멈추면 본 작업 5개가 "지연"이 아니라 조용한 무발송으로 바뀌는데, 야간 점검은 저녁 수집만 봐서 맥박·스캔이 사라져도 몰랐다. `ops_log.missing_sends(record)`가 그날 성적표에서 아침·맥박·스캔·저녁 누락 항목을 뽑고, 야간 점검(GitHub 예약이라 스케줄러와 무관하게 항상 돎)이 `SEND_MISSING=` 줄을 읽어 텔레그램에 경고 + PLAYBOOK 2.10 신설. 정상일엔 여전히 무발송(헛경보 0). 발송 계층(사고 6 유형) 전체를 덮는다 |
| V6.5-260909 | (번호만) | **발송 실패가 기록을 삼키던 구조 제거** (사고 9). 9/8 종가 스캔이 텔레그램 429(Too Many Requests, retry after 576)를 맞고 죽으면서, 이미 저장된 scans.json 기록이 커밋 전에 러너와 함께 증발했다. ① `update.yml` 종가 스캔 본·예비 2곳: python 종료코드를 `PIPESTATUS`로 받아두고 **커밋을 먼저 끝낸 뒤** 실패면 그때 🚨 통보 (기존엔 python이 exit 1이면 셸이 그 자리에서 멈춰 git 줄이 통째로 건너뛰어졌다) ② `closing_scan.send_telegram`에 notify.send와 같은 3회 재시도 + 429는 서버가 준 `retry_after`만큼 대기 (총 상한 600초, 판정은 `retry_after()` 순수 함수) ③ 야간 무발송 경고 문구에서 "스케줄러가 멈췄다" 단정 제거 ④ 테스트 4개 추가, 윈도우 cp949로 헛실패하던 테스트 2개에 `encoding="utf-8"` 명시 (로컬 125/125) |
| V6.6-260914 | (번호만) | **네이버 PC 금융 페이지 폐지 대응** (사고 10). 2026-09-11 저녁부터 `finance.naver.com/item/frgn.naver`(수급 표)·리서치 목록이 새 사이트 `stock.naver.com`으로 리다이렉트돼 HTML 파싱이 0건 → 품질 가드가 수집을 막아 9/11·9/14 마감 데이터가 비었다. ① 수급: `fetch_investor_flows` → 모바일 JSON `trend` API (`parse_trend_api`, 요청 수 동일) ② 차트 카드 120일 이력: `fetch_price_history` → 일봉 차트 API (`parse_chart_api`, 요청 6건→1건, 고가·저가 동봉하나 `chart_pack` 스키마는 그대로) ③ 리포트: `fetch_research_reports` → 리서치 JSON API (`parse_research_api`, 날짜 yy.mm.dd 유지). 셋 다 옛 HTML 파서를 비상용 폴백으로 보존 ④ 아침 자가복구 신선도를 "4일 이내"에서 "직전 거래일 마감분"으로 (`holidays_kr.last_trading_day`, update.yml). 금요일 실패를 월요일 아침이 통과시키던 구멍 제거. 테스트 5개 추가 (로컬 130/130) |
| V6.7-260914 | (번호만) | **W4 포모 방지기 + 검색식 2차 복기** (index.html 1개만 변경, 서버·스키마 무변경). `fomoNotes()`: 급등(+5%↑)·52주 신고가 돌파 당일 종목에 한해 실측 추격 성적(`chase_stats`·`phase_track`)을 악마의 변호인 반론 맨 앞·포트폴리오 담기 힌트·퀵메뉴 상자·AI 프롬프트 앵커에 표시. 표본 10건 미만은 "집계 중", ETF 제외, 조건 밖이면 0픽셀. 2026-07-14 검수에서 "임의 수치"로 반려됐던 도구를 실측이 쌓여 구현. 복기 결과는 `REVIEW-SCREENS-260914.md` (소유자 결정 대기 3건: 종합 수급 기본 꺼짐 · pair.html min_days 30 · S18 착수) |
| V6.8-260914 | (번호만) | **벤치 제도** (소유자 지시 2026-09-14: "개시 이후 성적이 나쁜 것은 추적은 하되 안 보이게, 중간 점검에서 살아나거나 연속으로 좋으면 부활"). 기준은 `collect.py CONFIG_BENCH` 한 곳: 벤치 = 🚪 신고가 문앞 · 🔥 종합 수급 · 🩸 마감 투매 눌림 · 🪜 조정 대기 매수. `build_screen_stats`가 `avg1`·최근 10일 창 `w1`/그 앞 10일 창 `w2`·`benched`/`revived`를 덧붙이고(키 추가만), `bench_flags`가 dump_stats·phase_track.pullback에 같은 플래그를 단다. 부활 = 창 2번 연속(표본 5↑·승률 55%↑·평균 +) 또는 살아남(최근 20일 표본 10↑·승률 60%↑·평균 +1%↑), 누적형은 표본 10↑·승률 50%↑·평균 +. 다시 나빠지면 자동 복귀. 읽는 쪽: index.html(⚙ 자동/켬/끔 순환, 🪑/🔁 태그, 저장 키 `kr_idea_screens2`) · notify.py `screen_lines`(벤치 제외, 부활 시 🔁) · closing_scan `show_dumps` · swing.html phaseSec 숨김. 테스트 4개 추가 |
| V6.9-260914 | (번호만) | **S18 리스크 예산 지킴이 + 동료주 표본 30일** (소유자 지시). ① index.html 포트폴리오 리스크 진단에 손실 한도 규칙: 종목(매수단가 대비, 기본 -7%)·계좌(전체 매입가 대비, 기본 -10%) 도달 🔴 · 한도의 60% 접근 ⚠️ + 보유 종목이 스윙 ⏱ 킬 스위치에 걸리면 교차 경고 (C3 손절 지킴이·S16 통합). 한도는 "총 평가액" 타일 ⚙ 줄 클릭 → 물음창 2개, 브라우저 저장(`kr_idea_risk_budget`). 전부 브라우저 판정·조건부 표시 ② pair.html `CFG.min_days` 15 → 30 (표본 외 검증 38%·-0.44%p 근거). 같은 날 V6.6~V6.9 통합 ZIP으로 전달 |
| V7.0-260915 | (번호만) | **분기 실적 태그 + 미국 10년물 한 줄** (하이브리드 스캐너 기획안 검수에서 채택 2건, IDEAS 검수 기록 참조). ① `collect.py` 2.5절: 네이버 모바일 `finance/quarter` API(키 불필요)로 최근 5개 확정 분기 매출·영업이익·순이익을 받아 `earnings.json`에 캐시(봇이 커밋, `CONFIG_EARN`: 종목별 7일 간격·하루 250종목 상한이라 평일 요청 증가는 100건 안팎). `earnings_tag`가 태그 하나(실적 성장·흑자 전환·적자 전환·연속 적자)와 YoY·QoQ를 `all_stocks[].earn`에 싣고, `build_earnings_stats`가 태그별 다음날 성적을 `earnings.stats`로 채점 (키 추가만). **점수·게이트 미반영, 표시·기록·채점만** (벤치 제도 절차. 적자 기업을 원천 배제하라는 원안은 일목 검수 ⑤와 같은 이유로 기각) ② index.html: 이름 옆 📈/🩹 배지(0픽셀·조건부), 악마의 변호인 반론(적자 전환·연속 적자·영업이익 -30%·매출 역성장), 퀵메뉴 한 줄, 비교기 3행(영업이익 YoY·매출 YoY·52주 저점 대비), AI 프롬프트 앵커, 통지표 ⑩행 ③ notify.py: 아침 브리핑 미국장 블록에 `미국10년물 4.96%▲`(야후 `^TNX` 폴백, `us_part`) + 5선 줄 끝 실적 태그(`earn_note`). 4.5% 임계 자동 대응 규칙은 기각(실측 4.98%라 상시 켜지는 상수) ④ `parse_integration`이 같은 응답에 오던 52주 최저가(`l52`)를 받아 `above_low52_pct` 기록 (요청 증가 0) ⑤ update.yml 커밋 4곳에 `git add earnings.json || true` 별도 줄 (없어도 history 커밋이 안 깨지게) ⑥ **🏷 배지 설명표** (소유자 지시: 배지가 늘어 분간이 어려움): 5선 점수 범례 줄 끝 "🏷 배지 설명 ▸" 클릭으로 펼침·접힘(`toggleBadgeGuide`, 이 기기에 저장 `kr_idea_badge_guide`), 푸터 링크(`showBadgeGuide`)로도 진입. 이름 옆 배지·신호 리본 칩·벤치/부활·통지표 등급 17종을 실제 모양 그대로 표 3개로. 기본 접힘 = 0픽셀, 새 배지를 만들면 `badgeGuideHtml`에 한 줄 추가할 것 ⑦ **모바일 다듬기** (소유자 지적: 설명표 글이 오른쪽으로 밀림): 원인은 시세표용 전역 `th,td{white-space:nowrap; text-align:right}` + `td:first-child{position:sticky}`가 설명표·퀵메뉴 시트 표에도 먹던 것. `.bguide td`와 `#qsheet th,td`에서 되돌려 통지표·비교기가 390px에서 가로 스크롤 없이 들어간다. 배지·칩을 **누르면 설명 토스트** (터치 기기엔 title 툴팁이 없어서. 단골·검색량·기사·NEW·N일째 title 문구 보강). 퀵메뉴 실적 한 줄은 값 없는 항목을 빼고 적자면 금액으로. 테스트 7개 추가 (로컬 141/141) |
| V7.1-260918 | (번호만) | **애프터마켓 대응: 정규장 기준 보정** (소유자 결정 2026-09-18: A 보강판 + 지난 기록 보정, `REVIEW-AFTERMARKET-260915.md` 7절). 2026-09-14부터 19:13 저녁 수집의 현재가·거래량·고저와 네이버 일봉·수급 자료의 지난날 종가가 애프터마켓 가격으로 바뀐 것을 collect.py 2.9절(`CONFIG_REGULAR`)이 되돌린다. ① 오늘 값: [3.2/5]에서 30분봉 1건/종목(ETF·ETN 제외, 하루 약 570건)으로 정규장 값 덮어쓰기, **점수화 전에** ② 지난날 종가: 수급 자료 `종가 - 전일대비` = 전날 정규장 종가 (요청 0). 스윙 21일·차트 카드 120일 종가의 9/14 이후 날짜 교체 ③ history 9/14~어제 가격·등락률·진입가·거래량·시고저 보정 (`backfill_history`, `regular_fix` 표시) ④ 매일 어제 기록 자가 대조 (`regular_check`) ⑤ data.json 키 추가 `regular_session`. **30분봉은 7거래일치만 남는다**. PLAYBOOK 2.12. 테스트 9개 추가 (로컬 150/150) |
| V7.2-260921 | (번호만) | **주간 결산 측정 교정 + 전환형 시험 선수 + 5선 벤치** (소유자 지시 2026-09-21: 주간 결산 승률 7%를 보고 조치 요구. 근거 문서 `REVIEW-SCORE-260921.md`). ① notify.py 주간 결산: 기존 승률은 지난 30일 선정분을 전부 오늘 가격 하나로 재서 같은 종목이 매일 뽑히면 한 묶음이 30번 세어진다. `ideas_vs_market()`이 history만 읽어 날짜당 한 번씩 센 "다음날·5거래일 뒤 기준 시장 평균(전 종목 단순 평균)을 이긴 날" 두 줄을 덧붙이고, 최근 5일 5선의 절반 이상이 한 섹터면 ⚠️ 쏠림 한 줄. 표시 오류 2건 수정: 최고의 날 `+-0.37%`(부호 서식 `:+`), KOSPI 주간이 월요일 등락을 빼먹던 것(`week_span`: 지난주 마지막 종가 기준. 9/14 주가 +3.14%가 아니라 -1.99%) ② 점수 축 복기 결과: 외국인 연속 순매수 7일 이상은 5일 뒤 시장 대비 -0.99%p(이긴 날 28%)로 세 구간 모두 역방향, 밸류는 장세를 타는 섹터 베팅(섹터 중립 0), CONFIG 숫자 조정 6종과 섹터 제한은 어느 것도 5선을 시장 평균 위로 못 올림 = **본 점수·가중치 무변경** ③ **전환형** (collect.py `STRATEGY_PRESETS` 5번째, `CONFIG_TURN`, `turn_flow_score`): 가중치는 기본형과 같고 외국인 연속일 배점만 1일 8·2일 14·3일 18·4~6일 8·7일↑ 0으로 바꾼 시험 선수. 본 점수(`score`·`flow_score`)에는 영향 없음. `build_strategy_race`는 그날 기록에 선수 키가 없으면 그날 all_stocks만으로 5선을 다시 뽑아 곡선을 잇고(미래 값 미사용), 합류일(`CONFIG_TURN["since"]` 2026-09-22) 이후만 따로 잰 맞대결을 `strategy_race.duel`에 싣는다(키 추가만). **합류 전 곡선(+7.8%)은 그 기록을 보고 만든 산식의 재계산값이라 판단 근거가 아니다. 판정은 duel로, 10월 중순(합류 후 15거래일↑)에** (9절 캘린더). 5일·10일 뒤 기준으로는 재계산에서도 개선이 없었다는 점도 복기 문서에 기록 ④ **5선 벤치** (`CONFIG_BENCH` ideas_* 4개, `build_ideas_bench` → data.json `ideas_bench` 키 추가): 최근 20거래일 동안 5선의 다음날 수익이 전 종목 평균을 이긴 날이 절반 미만이거나 평균 초과수익이 0 이하면 benched. 표본 15일 미만은 판정 안 함, 회복하면 자동 복귀, 채점·기록은 그대로. 읽는 쪽: index.html `ideasBenchNote`(제목을 "오늘의 관찰 목록 5선"으로, 🪑 사유 한 줄. 정상일 땐 0픽셀) · notify.py 아침(제목+한 줄)·저녁("관찰 목록 5선:"). 9/18 기록 실판정: 20일 중 7일·평균 -0.5%p = 벤치 ⑤ index.html 부수: 리그 곡선 5번째 색, `duelNote` 안내 한 줄, 🏆 따라가기 버튼은 가중치 조합이 아닌 선수를 건너뜀. notify 주간 순위의 전환형 줄에 "시험 선수 · 합류 전 구간은 재계산값" 꼬리표, S13 튜닝 제안에서 전환형 제외. backtest.html 프리셋에는 넣지 않았다(가중치 조합이 아니라서). 테스트 4개 추가 (로컬 154/154) |

## 아키텍처 (파일 전부 저장소 최상위)

| 파일 | 역할 | 수정 빈도 |
|---|---|---|
| `collect.py` | 수집·점수화 파이프라인. **튜닝은 상단 CONFIG만** | 튜닝 시 |
| `notify.py` | 텔레그램 발송 (기본/`--evening`/`--weekly`/`--dry-run`, 미국장 블록) | 드묾 |
| `closing_scan.py` | 14:48 종가매매 스캔 + `--pulse`(점심 맥박) (튜닝: CONFIG_SCAN) | 튜닝 시 |
| `us_kr_map.json` | 미국↔국내 연동주 매핑 | 분기 점검 |
| `ROADMAP.md` | 남은 기획·의사결정 기록 | 기획 변경 시 |
| `HANDOFF.md` | 세션 인수인계 요약 (새 세션이 가장 먼저 읽는 문서) | 버전 확정 시 |
| `PLAYBOOK.md` | 장애 증상별 복구 매뉴얼 | 사고 발생 시 |
| `IDEAS.md` | 아이디어 뱅크 (기획 초안 보관소) | 수시 |
| `index.html` | 대시보드 전체 (단일 파일, 외부 JS 라이브러리 없음) | 기능 추가 시 |
| `swing.html` · `dividend.html` · `insider.html` · `mine.html` | 스윙·배당·내부자·지뢰 하위 페이지 | 기능 추가 시 |
| `data.json` | 최신 수집 결과 (봇이 커밋) | 자동 |
| `history/*.json` | 일별 스냅샷 — 성과 트래킹·추세의 재료. **삭제 금지** | 자동 |
| `test_parsers.py` | 오프라인 테스트. 수집 전 Actions에서 자동 실행 | 코드 수정 시 |
| `ops_log.py` / `ops.json` | 실행 성적표 기록기 / 장부 (야간 자가 점검이 커밋) | 드묾 / 자동 |
| `earnings.json` | 분기 실적 캐시 (V7.0, 저녁 수집이 종목별 7일 간격으로 갱신·커밋). 직접 수정 금지, 지워도 3일이면 다시 찬다 | 자동 |
| `holidays_kr.py` | KRX 휴장일 달력 — 휴장일엔 수집·스캔·저녁요약·자가점검이 조용히 쉼, 아침 브리핑엔 안내 (2027년분은 월력요항 기준 선등록 완료, 12월에 KRX 공지 대조만) | 매년 12월 목록 추가 |
| `chart.html` | 📊 차트 카드 (120일 차트+판정 4줄, 대상: 5선·스윙·워치리스트 → data.json `chart_pack`) | 기능 추가 시 |
| `pair.html` | 🤝 동료주 발굴기 (그림자 페어 · history의 all_stocks를 브라우저에서 내려받아 시차 상관 계산, 서버 연산 0원) | 기능 추가 시 |
| `backtest.html` | 🧪 백테스트 실험실 (가중치 조합의 과거 성과를 브라우저에서 재계산 · 전략 리그와 같은 산식) | 기능 추가 시 |
| `watchlist.txt` | 텔레그램 관심종목 (6자리 코드, # 주석) | 사용자가 |

## 절대 규칙 (어기면 시스템이 깨진다)

1. **data.json 스키마는 하위호환 유지.** 키 삭제·이름 변경 금지 (추가만 허용).
   index.html과 notify.py가 과거 형식도 읽도록 폴백이 있는데, 새 코드도 같은 원칙을 따를 것.
2. **모든 코드 수정 후 `python test_parsers.py` 전체 통과 확인.** 새 기능엔 테스트 추가.
   Actions가 수집 전에 테스트를 돌리므로, 테스트가 깨지면 갱신 자체가 안 된다 (의도된 안전장치).
3. **휴장일 스킵과 품질 가드 로직을 우회하지 말 것** (`is_holiday_rerun`, `validate_collection`).
   이게 엉터리 데이터 배포를 막는 핵심 안전장치다.
4. **한국 주식 색 관례: 빨강=상승, 파랑=하락.** 서구식으로 뒤집지 말 것.
5. **네이버 요청 예의**: `REQUEST_DELAY`(0.12s) 제거 금지, 유니버스 무리하게 확대 금지.
6. **시크릿을 코드·파일·로그에 절대 노출하지 말 것.** 환경변수로만 접근.
7. **점수 산식 변경은 CONFIG 값 조정으로.** 산식 구조 자체를 바꿀 땐 README의 산식 설명도 갱신.
8. **모든 도구는 API 호출 비용 0원.** 유료 API(LLM API 포함)는 지금 도구든 앞으로 만들
   도구든 절대 사용·제안하지 말 것 (소유자 확정 방침, 2026-07-12). 무료 쿼터가 있는
   공식 API(네이버 개발자, OpenDART 등)만 허용. 문맥 이해·자유 논평이 필요한 작업은
   자동화하지 말고, 소유자가 이미 구독 중인 Claude/Gemini 대화에 직접 요청하는 방식으로 안내.
9. **엠대시(—) 사용 금지** (소유자 확정 방침, 2026-07-24). 사용자가 보게 되는 모든 텍스트,
   즉 대시보드 화면·텔레그램 메시지·Actions 로그 문구에는 가운뎃점(·)·쉼표·콜론만 쓴다.
   기획 문서(`.md`)와 코드 주석은 예외. 새 문구를 넣기 전 `—` 문자를 검색해 확인할 것.
10. **0픽셀 원칙.** 신규 기능은 상시 화면 길이를 늘리지 않는다 (클릭 확장·조건부 표시만).
11. **제안 전 결정 기록 대조 의무** (2026-09-01 신설, 실사고). 새 도구·외부 서비스·구조 변경을
    제안하기 전에 `HANDOFF.md` 10절(결정 기록)과 12절(사용자 액션 대기)을 **전문으로 읽고**
    충돌 항목이 없음을 확인한다. 충돌하면 제안 대신 **"이런 결정이 이미 있습니다"를 먼저 보고**하고
    소유자 판단을 기다린다. 문서를 grep으로 조각만 보고 제안하는 것을 금지한다.
    2026-09-01에 AI가 10절이 '다시 꺼내지 말 것'으로 못 박은 외부 스케줄러 안을 그대로 제안했고,
    소유자가 사후 승인해 유지됐지만 절차는 어긴 것이다. 같은 실수 반복 금지.

## 배포 방법 (중요 — 특수한 환경)

AI 세션에서 git push가 막혀 있을 수 있다. 그 경우 배포 절차:
- 수정 파일들을 ZIP으로 사용자에게 전달 → 사용자가 GitHub 웹에서
  `Add file → Upload files`로 덮어쓰기 (같은 파일명 자동 교체)
- `.github/` 안의 파일은 드래그 업로드가 안 되므로, 웹 편집 링크
  (`github.com/km-jang/kr-idea/edit/main/.github/workflows/update.yml`)로 붙여넣기 안내
- **코드 파일(.py·.html 등)은 조각 붙여넣기(부분 삽입) 절대 금지. 전체 파일 교체만.**
  웹 편집기에서 Ctrl+A → Delete → 전체 붙여넣기. 부분 삽입은 줄바꿈 하나만 증발해도
  전체 자동화가 선다 (2026-07-31 실사고: test_parsers.py 조각 삽입에서 마지막 줄바꿈이
  사라져 SyntaxError → 수집이 이틀 전면 중단. 테스트 게이트가 설계대로 수집을 차단했다)
- 전달 전 반드시: 테스트 전체 통과 + (UI 변경 시) 로컬 렌더링 확인
- 반영 후 `raw.githubusercontent.com`으로 파일 일치 검증 (캐시 지연 몇 분 감안)

## 미세조정(튜닝) 요청을 받으면

1. `collect.py` 상단 CONFIG의 해당 숫자만 수정 (README '튜닝 가이드' 참고)
2. `python test_parsers.py` 통과 확인
3. `python collect.py --sample`로 샘플 생성 후 로직 이상 없는지 확인
4. collect.py 하나만 전달, 사용자에게 업로드 안내
5. 가중치(수급/밸류/모멘텀/공시 비중) 얘기라면 코드 수정 전에
   **대시보드 슬라이더로 먼저 실험**하도록 권할 것

## 검증 체크리스트 (모든 작업 공통)

- [ ] test_parsers.py 전체 통과
- [ ] `collect.py --sample` 정상 생성 (스키마 키 확인)
- [ ] UI 변경 시: 라이트/다크/모바일(390px) 렌더링 + 콘솔 에러 0
- [ ] notify 변경 시: `--dry-run`으로 3종 메시지 미리보기, 4096자 미만
- [ ] update.yml 변경 시: YAML 파싱 + 스케줄별 step 조건 확인 + 자정 넘김 보정 유지 확인
- [ ] 사용자 노출 텍스트에 엠대시(`—`)가 없는지 검색 확인 (규칙 9)
- [ ] 사용자 안내는 클릭 단위로, 파일 몇 개를 올리는지 명시

## 장애 발생 시

`PLAYBOOK.md`의 증상별 매뉴얼을 따를 것.
